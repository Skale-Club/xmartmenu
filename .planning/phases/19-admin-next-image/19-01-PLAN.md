---
phase: 19-admin-next-image
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(admin)/settings/branding/BrandingClient.tsx
autonomous: true
requirements:
  - IMG-04

must_haves:
  truths:
    - "BrandingClient.tsx contains zero bare <img> tags"
    - "Logo preview renders using next/image with fixed width=80 height=80"
    - "Banner preview renders using next/image with fill + sizes='100vw' inside a relative-positioned parent"
    - "npm run build exits 0 after changes"
  artifacts:
    - path: "src/app/(admin)/settings/branding/BrandingClient.tsx"
      provides: "Admin branding settings UI — logo and banner upload/preview"
      contains: "import Image from 'next/image'"
  key_links:
    - from: "BrandingClient.tsx logo preview"
      to: "next/image Image"
      via: "width={80} height={80} objectFit via className"
      pattern: "Image.*width=\\{80\\}.*height=\\{80\\}"
    - from: "BrandingClient.tsx banner preview"
      to: "next/image Image"
      via: "fill sizes='100vw' inside relative div"
      pattern: "Image.*fill.*sizes"
---

<objective>
Replace the two bare `<img>` tags in BrandingClient.tsx with `next/image` `<Image>` components.

Purpose: Satisfy IMG-04 — admin branding previews must use Next.js Image Optimization for automatic WebP conversion, correct sizing hints, and LCP improvement.
Output: BrandingClient.tsx with Image import and two migrated image elements; zero `<img>` tags remain.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- Current img locations in BrandingClient.tsx — exact lines to replace -->

Line 114 (logo preview — inside w-20 h-20 div):
```tsx
{logoUrl ? <img src={logoUrl} alt="Logo" className="w-full h-full object-contain" /> : <span className="text-3xl">🏪</span>}
```
Parent div (line 113): `<div className="w-20 h-20 rounded-xl border border-zinc-200 bg-zinc-50 flex items-center justify-center overflow-hidden flex-shrink-0">`

Line 133 (banner preview — inside w-full h-28 div):
```tsx
<img src={bannerUrl} alt="Banner" className="w-full h-full object-cover" />
```
Parent div (line 132): `<div className="w-full h-28 rounded-xl border border-zinc-200 overflow-hidden mb-3">`

next/image import already present? NO — must add:
```tsx
import Image from 'next/image'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate logo and banner img tags to next/image in BrandingClient.tsx</name>
  <files>src/app/(admin)/settings/branding/BrandingClient.tsx</files>
  <action>
    1. Add `import Image from 'next/image'` after the existing imports (after line 4, `import type { TenantSettings } from '@/types/database'`).

    2. Replace the logo `<img>` (line 114):
       OLD: `<img src={logoUrl} alt="Logo" className="w-full h-full object-contain" />`
       NEW: `<Image src={logoUrl} alt="Logo" width={80} height={80} className="object-contain" />`
       — The parent div `w-20 h-20` (80px) provides the fixed container; Image renders at exactly 80x80. Remove `w-full h-full` classes since fixed width/height replaces them.

    3. Replace the banner `<img>` (line 133):
       OLD: `<img src={bannerUrl} alt="Banner" className="w-full h-full object-cover" />`
       NEW: `<Image src={bannerUrl} alt="Banner" fill sizes="100vw" className="object-cover" />`
       — The parent div (line 132) must gain `relative` class so `fill` positioning works. Change:
       OLD: `<div className="w-full h-28 rounded-xl border border-zinc-200 overflow-hidden mb-3">`
       NEW: `<div className="relative w-full h-28 rounded-xl border border-zinc-200 overflow-hidden mb-3">`

    No other changes. The supabase.co remotePatterns in next.config.ts already cover these URLs.
  </action>
  <verify>
    <automated>cd /c/Users/Vanildo/Dev/xmartmenu && grep -n "&lt;img" src/app/\(admin\)/settings/branding/BrandingClient.tsx | wc -l</automated>
  </verify>
  <done>
    - `grep -n '<img' src/app/(admin)/settings/branding/BrandingClient.tsx` returns 0 matches
    - File contains `import Image from 'next/image'`
    - Logo Image uses `width={80} height={80}`
    - Banner parent div has `relative` class; banner Image uses `fill sizes="100vw"`
    - `npm run build` exits 0
  </done>
</task>

</tasks>

<verification>
```bash
# Zero img tags remain
grep -n '<img' src/app/\(admin\)/settings/branding/BrandingClient.tsx
# Expected: no output

# next/image import present
grep 'import Image' src/app/\(admin\)/settings/branding/BrandingClient.tsx
# Expected: import Image from 'next/image'

# Build passes
npm run build 2>&1 | tail -5
```
</verification>

<success_criteria>
- Zero `<img` occurrences in BrandingClient.tsx
- `Image` component used with correct props: logo `width={80} height={80}`, banner `fill sizes="100vw"` on `relative` parent
- `npm run build` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/19-admin-next-image/19-01-SUMMARY.md`
</output>
