---
phase: 19-admin-next-image
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(superadmin)/tenants/TenantsClient.tsx
  - src/app/(superadmin)/tenants/[id]/TenantDetailClient.tsx
autonomous: true
requirements:
  - IMG-06

must_haves:
  truths:
    - "TenantsClient.tsx contains zero bare <img> tags"
    - "TenantDetailClient.tsx contains zero bare <img> tags"
    - "Tenant logo thumbnails in superadmin tenant list use next/image with width=40 height=40"
    - "Tenant logo in superadmin tenant detail header uses next/image with width=40 height=40"
    - "npm run build exits 0 after changes"
  artifacts:
    - path: "src/app/(superadmin)/tenants/TenantsClient.tsx"
      provides: "Superadmin tenant list UI"
      contains: "import Image from 'next/image'"
    - path: "src/app/(superadmin)/tenants/[id]/TenantDetailClient.tsx"
      provides: "Superadmin tenant detail UI"
      contains: "import Image from 'next/image'"
  key_links:
    - from: "TenantsClient.tsx tenant row logo"
      to: "next/image Image"
      via: "width={40} height={40} inside w-10 h-10 div"
      pattern: "Image.*width=\\{40\\}.*height=\\{40\\}"
    - from: "TenantDetailClient.tsx header logo"
      to: "next/image Image"
      via: "width={40} height={40} with rounded-lg object-contain border"
      pattern: "Image.*width=\\{40\\}.*height=\\{40\\}"
---

<objective>
Replace the bare `<img>` tags in TenantsClient.tsx and TenantDetailClient.tsx with `next/image` `<Image>` components.

Purpose: Satisfy IMG-06 — superadmin tenant logos must use Next.js Image Optimization for WebP conversion and correct sizing hints.
Output: Both files with Image import and migrated image elements; zero `<img>` tags remain in either file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- Current img locations — exact lines to replace -->

TenantsClient.tsx line 400 (tenant logo in list row — inside w-10 h-10 rounded-lg overflow-hidden div):
```tsx
{client.logo_url
  ? <img src={client.logo_url} alt={client.name!} className="w-full h-full object-contain" />
  : <span className="text-lg">🏪</span>}
```
Parent div (line 398): `<div className="w-10 h-10 rounded-lg bg-zinc-100 flex items-center justify-center overflow-hidden flex-shrink-0">`

TenantDetailClient.tsx line 372 (tenant logo in detail header — standalone conditional):
```tsx
{tenant.logo_url
  ? <img src={tenant.logo_url} alt={tenant.name} className="w-10 h-10 rounded-lg object-contain border border-zinc-100" />
  : <div className="w-10 h-10 rounded-lg bg-zinc-100 flex items-center justify-center text-lg">🏪</div>}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate tenant logo img tag to next/image in TenantsClient.tsx</name>
  <files>src/app/(superadmin)/tenants/TenantsClient.tsx</files>
  <action>
    1. Add `import Image from 'next/image'` after the existing imports (after line 6, the ConfirmDialog import).

    2. Replace the logo `<img>` (line 400):
       OLD: `? <img src={client.logo_url} alt={client.name!} className="w-full h-full object-contain" />`
       NEW: `? <Image src={client.logo_url} alt={client.name!} width={40} height={40} className="object-contain" />`
       — `w-10` = 40px. Remove `w-full h-full` since fixed dimensions replace them. The parent div `w-10 h-10 overflow-hidden` still clips the Image. Keep `object-contain`.

    No other changes.
  </action>
  <verify>
    <automated>cd /c/Users/Vanildo/Dev/xmartmenu && grep -c '<img' src/app/\(superadmin\)/tenants/TenantsClient.tsx || echo "0"</automated>
  </verify>
  <done>
    - `grep -n '<img' src/app/(superadmin)/tenants/TenantsClient.tsx` returns 0 matches
    - File contains `import Image from 'next/image'`
    - Logo Image uses `width={40} height={40}`
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate tenant logo img tag to next/image in TenantDetailClient.tsx</name>
  <files>src/app/(superadmin)/tenants/[id]/TenantDetailClient.tsx</files>
  <action>
    1. Add `import Image from 'next/image'` after the existing imports (after line 4, `import ConfirmDialog from '@/components/ui/ConfirmDialog'`).

    2. Replace the logo `<img>` (line 372):
       OLD: `? <img src={tenant.logo_url} alt={tenant.name} className="w-10 h-10 rounded-lg object-contain border border-zinc-100" />`
       NEW: `? <Image src={tenant.logo_url} alt={tenant.name} width={40} height={40} className="rounded-lg object-contain border border-zinc-100" />`
       — `w-10` = 40px. Remove `w-10 h-10` from className since fixed dimensions replace them. Keep `rounded-lg object-contain border border-zinc-100`.

    No other changes.
  </action>
  <verify>
    <automated>cd /c/Users/Vanildo/Dev/xmartmenu && grep -c '<img' "src/app/(superadmin)/tenants/[id]/TenantDetailClient.tsx" || echo "0"</automated>
  </verify>
  <done>
    - `grep -n '<img' src/app/(superadmin)/tenants/[id]/TenantDetailClient.tsx` returns 0 matches
    - File contains `import Image from 'next/image'`
    - Logo Image uses `width={40} height={40}`
    - `npm run build` exits 0 (run after both tasks complete)
  </done>
</task>

</tasks>

<verification>
```bash
# Zero img tags in both files
grep -n '<img' src/app/\(superadmin\)/tenants/TenantsClient.tsx
grep -n '<img' "src/app/(superadmin)/tenants/[id]/TenantDetailClient.tsx"
# Expected: no output from either

# next/image imports present
grep 'import Image' src/app/\(superadmin\)/tenants/TenantsClient.tsx
grep 'import Image' "src/app/(superadmin)/tenants/[id]/TenantDetailClient.tsx"

# Build passes
npm run build 2>&1 | tail -5
```
</verification>

<success_criteria>
- Zero `<img` occurrences in TenantsClient.tsx and TenantDetailClient.tsx
- Both files import `Image from 'next/image'`
- Tenant logos use `width={40} height={40}` with appropriate className
- `npm run build` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/19-admin-next-image/19-03-SUMMARY.md`
</output>
