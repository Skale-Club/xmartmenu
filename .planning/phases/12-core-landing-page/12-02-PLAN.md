---
phase: 12-core-landing-page
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - src/app/(marketing)/layout.tsx
  - src/app/(marketing)/page.tsx
  - src/app/page.tsx
autonomous: true
requirements:
  - LP-01
  - LP-02
  - LP-03
  - LP-04
  - LP-05

must_haves:
  truths:
    - "Visitor at / sees hero with 'Your restaurant menu, online in minutes' — not a login redirect"
    - "'Get started free' CTA links to /auth/register"
    - "'How It Works' section shows 3 numbered steps"
    - "4 feature blocks render in 2x2 grid on desktop"
    - "FAQ accordion has 6 questions using native details/summary (zero JS)"
    - "Footer shows Privacy Policy and Terms of Service links"
    - "No 'use client' directive appears anywhere in page.tsx or layout.tsx"
    - "export const dynamic = 'force-static' is present in page.tsx"
  artifacts:
    - path: "src/app/(marketing)/page.tsx"
      provides: "Full landing page — all sections in order"
      contains: "export const dynamic = 'force-static'"
      exports: ["default"]
    - path: "src/app/(marketing)/layout.tsx"
      provides: "Isolated marketing layout — html lang=en, Inter swap"
      exports: ["default", "metadata"]
    - path: "src/app/page.tsx"
      provides: "Root passthrough — cleared of old redirect"
      contains: "export { default } from './(marketing)/page'"
  key_links:
    - from: "src/app/(marketing)/page.tsx"
      to: "/auth/register"
      via: "href on CTA anchor elements"
      pattern: "href=\"/auth/register\""
    - from: "src/app/(marketing)/layout.tsx"
      to: "Inter font"
      via: "next/font/google with display swap"
      pattern: "display: 'swap'"
---

<objective>
Build the complete landing page: the `(marketing)` route group layout and the full page component with all 7 sections (nav, hero, how-it-works, feature blocks, FAQ, footer CTA band, footer).

Purpose: This is the core deliverable of Phase 12 — the page a visitor sees when landing on xmartmenu.skale.club. All copy is locked from CONTEXT.md D-10 through D-21. All visual classes are locked from UI-SPEC.md. Zero interpretation needed.

Output:
- src/app/(marketing)/layout.tsx — isolated layout, html lang="en", Inter display:swap, page-level metadata
- src/app/(marketing)/page.tsx — static landing page, all sections, force-static
- src/app/page.tsx — root passthrough (re-exports marketing page)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-core-landing-page/12-CONTEXT.md
@.planning/phases/12-core-landing-page/12-UI-SPEC.md
@.planning/phases/12-core-landing-page/12-01-SUMMARY.md

<interfaces>
<!-- Design system locked values from UI-SPEC.md — do not deviate -->

TYPOGRAPHY (4 roles only):
- Body:        text-base font-normal leading-relaxed       (16px/400)
- Small:       text-sm font-normal                         (14px/400)
- Subheading:  text-xl font-bold                           (20px/700)
- Heading:     text-3xl font-bold (sections)               (28px/700)
             text-4xl sm:text-5xl font-bold leading-tight  (hero)

COLORS (60/30/10):
- Page bg:     bg-white
- Band bg:     bg-zinc-50 (hero, feature blocks)
- Band bg:     bg-white   (how-it-works, FAQ)
- Accent fill: bg-zinc-900 text-white    (CTA buttons, step circles, nav logo)
- Text primary:   text-zinc-900
- Text secondary: text-zinc-700
- Text body:      text-zinc-600
- Text muted:     text-zinc-500
- Border:         border-zinc-200

SPACING:
- Section padding:       py-16 px-4
- Hero padding:          py-24 px-4 (approved exception)
- Content max-width:     max-w-5xl mx-auto (feature grid)
- Narrow max-width:      max-w-2xl mx-auto (hero, FAQ, footer CTA band)
- Responsive gutters:    mx-auto px-4 sm:px-6

ICONS (lucide-react — exact assignments):
- Multi-language menus: Globe
- QR code:              QrCode
- AI-powered setup:     Sparkles
- Online ordering:      ShoppingCart
- Step 1 (account):     UserPlus
- Step 2 (menu):        UtensilsCrossed
- Step 3 (QR):          QrCode
- FAQ chevron:          ChevronDown
- Footer Instagram:     Instagram
- Footer WhatsApp:      MessageCircle

ANTI-PATTERNS (hard prohibition):
- NO 'use client' anywhere in this plan's files
- NO fake testimonials or fake metrics
- NO next/script
- NO autoPlay video or animated GIF
- NO hero image (background is bg-zinc-50 only)
- CTA buttons are <a href="..."> anchors, not <button> elements

COPYWRITING (exact — from UI-SPEC.md Copywriting Contract):
Nav:        logo="XmartMenu", cta="Get started"
Hero:       headline="Your restaurant menu, online in minutes"
            subheadline="Create a beautiful digital menu, generate a QR code, and start taking orders — no tech skills needed."
            cta="Get started free", microcopy="No credit card required."
HiW:        heading="How It Works"
            step1="Create your account" / "Sign up in seconds, no credit card required."
            step2="Build your menu" / "Add categories, products, images and prices. We'll help with AI."
            step3="Share your QR code" / "Print it, display it at the table, and let customers browse."
Features:   heading="Everything your restaurant needs"
            f1="Multi-language menus" / "Serve customers in Portuguese, English, Spanish and more — your menu adapts to every language automatically."
            f2="QR code, ready in seconds" / "Every menu gets a unique QR code. Print it, share it, or embed it anywhere."
            f3="AI-powered setup" / "Our team can populate your entire menu in minutes using AI — categories, descriptions, and photos included."
            f4="Online ordering" / "Let customers order directly from the table. Available as an add-on."
FAQ:        heading="Frequently asked questions"
            q1="Is it free?" / "Yes, xmartmenu is free during beta. We'll announce pricing changes with advance notice."
            q2="Do I need a developer?" / "No. You set up your menu from a simple admin panel — no code required."
            q3="Can my menu be in multiple languages?" / "Yes. You can enable multiple languages and your menu content will be available in each."
            q4="What happens to my data if I cancel?" / "Your data is yours. Contact us and we'll export everything before you leave."
            q5="How do QR codes work?" / "Every menu gets a unique QR code. Customers scan it and see your live menu instantly."
            q6="Is online ordering available?" / "Online ordering is available as an add-on. Contact us to enable it for your restaurant."
FooterCTA:  heading="Ready to get started?", body="Join the first restaurants using xmartmenu.", cta="Get started free"
Footer:     tagline="Cardápio digital para restaurantes"
            legal group: "Legal" / "Privacy Policy" → /privacy / "Terms of Service" → /terms
            copyright="© 2026 xmartmenu. All rights reserved."
            social: Instagram (href="#" aria-label="Instagram"), MessageCircle (href="#" aria-label="WhatsApp")
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create (marketing)/layout.tsx — isolated marketing layout</name>
  <read_first>
    - src/app/layout.tsx (existing root layout — for reference, do not copy lang or metadata verbatim)
    - .planning/phases/12-core-landing-page/12-CONTEXT.md (decisions D-01, D-02, D-03, D-23, D-25)
    - .planning/phases/12-core-landing-page/12-UI-SPEC.md (Font section, Performance Contracts section)
  </read_first>
  <files>
    src/app/(marketing)/layout.tsx
  </files>
  <action>
Create the directory `src/app/(marketing)/` and the file `src/app/(marketing)/layout.tsx`.

The (marketing) route group gives the landing page an isolated `<html lang="en">`, its own font config, and its own metadata — preventing admin/SaaS styles and pt-BR lang from bleeding in (D-01).

```typescript
import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import '../globals.css'

const inter = Inter({
  subsets: ['latin'],
  display: 'swap',
  preload: true,
})

export const metadata: Metadata = {
  title: 'XmartMenu — Your restaurant menu, online in minutes',
  description:
    'Create a beautiful digital menu, generate a QR code, and start taking orders — no tech skills needed.',
  metadataBase: new URL('https://xmartmenu.skale.club'),
  openGraph: {
    title: 'XmartMenu — Your restaurant menu, online in minutes',
    description:
      'Create a beautiful digital menu, generate a QR code, and start taking orders — no tech skills needed.',
    url: 'https://xmartmenu.skale.club',
    siteName: 'XmartMenu',
    locale: 'en_US',
    type: 'website',
  },
  twitter: {
    card: 'summary_large_image',
    title: 'XmartMenu — Your restaurant menu, online in minutes',
    description:
      'Create a beautiful digital menu, generate a QR code, and start taking orders — no tech skills needed.',
  },
}

export default function MarketingLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" className="h-full antialiased">
      <body className={`${inter.className} min-h-full bg-white`}>
        {children}
      </body>
    </html>
  )
}
```

IMPORTANT: Analytics and SpeedInsights are NOT added here — they are already in the root layout.tsx (added in Plan 01). Adding them here would duplicate the scripts.

IMPORTANT: The `import '../globals.css'` path must resolve from `src/app/(marketing)/` to `src/app/globals.css` — the relative path `../globals.css` is correct.

IMPORTANT: Do NOT add `'use client'` — this is a Server Component layout (D-25).
  </action>
  <verify>
    <automated>
      grep -n "lang=\"en\"" /c/Users/Vanildo/Dev/xmartmenu/src/app/\(marketing\)/layout.tsx
      grep -n "display: 'swap'" /c/Users/Vanildo/Dev/xmartmenu/src/app/\(marketing\)/layout.tsx
      grep -n "metadataBase" /c/Users/Vanildo/Dev/xmartmenu/src/app/\(marketing\)/layout.tsx
      grep -n "use client" /c/Users/Vanildo/Dev/xmartmenu/src/app/\(marketing\)/layout.tsx
    </automated>
  </verify>
  <acceptance_criteria>
    - src/app/(marketing)/layout.tsx contains `lang="en"` (not pt-BR)
    - src/app/(marketing)/layout.tsx contains `display: 'swap'`
    - src/app/(marketing)/layout.tsx contains `metadataBase: new URL('https://xmartmenu.skale.club')`
    - src/app/(marketing)/layout.tsx does NOT contain `'use client'`
    - src/app/(marketing)/layout.tsx does NOT contain `Analytics` or `SpeedInsights` imports (already in root layout)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Marketing layout created with lang="en", Inter display:swap, page-level metadata</done>
</task>

<task type="auto">
  <name>Task 2: Create (marketing)/page.tsx — full landing page all sections</name>
  <read_first>
    - src/app/(marketing)/layout.tsx (just created in Task 1)
    - .planning/phases/12-core-landing-page/12-UI-SPEC.md (ALL sections — read every pixel value)
    - .planning/phases/12-core-landing-page/12-CONTEXT.md (ALL decisions D-10 through D-21, anti-patterns)
    - .planning/research/PITFALLS.md (Pitfall 4 — no use client above fold, Pitfall 2 — force-static)
  </read_first>
  <files>
    src/app/(marketing)/page.tsx
    src/app/page.tsx
  </files>
  <action>
Create `src/app/(marketing)/page.tsx` with ALL 7 sections in exact order. Then update `src/app/page.tsx` to re-export from the marketing page.

**CRITICAL constraints before writing any line:**
- `export const dynamic = 'force-static'` MUST be the first export in the file (D-02)
- NO `'use client'` directive anywhere — this entire file is a Server Component (D-25, Pitfall 4)
- All CTA buttons are `<a href="/auth/register">` anchor tags, NOT `<button>` elements
- FAQ uses native `<details>/<summary>` — zero JS (D-18)
- No images in hero — background is `bg-zinc-50` only (Pitfall 4, UI-SPEC)

**Full file content for `src/app/(marketing)/page.tsx`:**

```typescript
export const dynamic = 'force-static'

import { Globe, QrCode, Sparkles, ShoppingCart, ChevronDown, Instagram, MessageCircle, UserPlus, UtensilsCrossed } from 'lucide-react'

// ─── Section data (all copy locked from CONTEXT.md D-10 through D-21) ───────

const steps = [
  {
    num: 1,
    icon: UserPlus,
    title: 'Create your account',
    body: 'Sign up in seconds, no credit card required.',
  },
  {
    num: 2,
    icon: UtensilsCrossed,
    title: 'Build your menu',
    body: "Add categories, products, images and prices. We'll help with AI.",
  },
  {
    num: 3,
    icon: QrCode,
    title: 'Share your QR code',
    body: 'Print it, display it at the table, and let customers browse.',
  },
]

const features = [
  {
    icon: Globe,
    title: 'Multi-language menus',
    body: 'Serve customers in Portuguese, English, Spanish and more — your menu adapts to every language automatically.',
  },
  {
    icon: QrCode,
    title: 'QR code, ready in seconds',
    body: 'Every menu gets a unique QR code. Print it, share it, or embed it anywhere.',
  },
  {
    icon: Sparkles,
    title: 'AI-powered setup',
    body: 'Our team can populate your entire menu in minutes using AI — categories, descriptions, and photos included.',
  },
  {
    icon: ShoppingCart,
    title: 'Online ordering',
    body: 'Let customers order directly from the table. Available as an add-on.',
  },
]

const faqs = [
  {
    q: 'Is it free?',
    a: "Yes, xmartmenu is free during beta. We'll announce pricing changes with advance notice.",
  },
  {
    q: 'Do I need a developer?',
    a: 'No. You set up your menu from a simple admin panel — no code required.',
  },
  {
    q: 'Can my menu be in multiple languages?',
    a: 'Yes. You can enable multiple languages and your menu content will be available in each.',
  },
  {
    q: 'What happens to my data if I cancel?',
    a: "Your data is yours. Contact us and we'll export everything before you leave.",
  },
  {
    q: 'How do QR codes work?',
    a: 'Every menu gets a unique QR code. Customers scan it and see your live menu instantly.',
  },
  {
    q: 'Is online ordering available?',
    a: 'Online ordering is available as an add-on. Contact us to enable it for your restaurant.',
  },
]

// ─── Components ──────────────────────────────────────────────────────────────

function Nav() {
  return (
    <nav className="sticky top-0 z-50 h-16 bg-white border-b border-zinc-200 flex items-center px-4 sm:px-6">
      <div className="max-w-5xl mx-auto w-full flex items-center justify-between">
        <span className="text-xl font-bold text-zinc-900">XmartMenu</span>
        <a
          href="/auth/register"
          className="bg-zinc-900 text-white px-4 py-2 rounded-lg text-sm font-bold hover:bg-zinc-800 transition-colors min-h-[44px] inline-flex items-center focus:outline-none focus:ring-2 focus:ring-zinc-900 focus:ring-offset-2"
        >
          Get started
        </a>
      </div>
    </nav>
  )
}

function Hero() {
  return (
    <section className="bg-zinc-50 py-24 px-4">
      <div className="max-w-2xl mx-auto text-center">
        <h1 className="text-4xl sm:text-5xl font-bold text-zinc-900 leading-tight">
          Your restaurant menu, online in minutes
        </h1>
        <p className="text-xl text-zinc-600 mt-4 leading-relaxed">
          Create a beautiful digital menu, generate a QR code, and start taking orders — no tech skills needed.
        </p>
        <div className="mt-8">
          <a
            href="/auth/register"
            className="inline-flex items-center justify-center bg-zinc-900 text-white px-8 py-3.5 rounded-xl text-base font-bold hover:bg-zinc-800 transition-colors min-h-[44px] focus:outline-none focus:ring-2 focus:ring-zinc-900 focus:ring-offset-2"
          >
            Get started free
          </a>
        </div>
        <p className="mt-3 text-sm text-zinc-500">No credit card required.</p>
      </div>
    </section>
  )
}

function HowItWorks() {
  return (
    <section className="bg-white py-16 px-4">
      <div className="max-w-5xl mx-auto">
        <h2 className="text-3xl font-bold text-zinc-900 text-center mb-12">How It Works</h2>
        <div className="grid grid-cols-1 sm:grid-cols-3 gap-8">
          {steps.map(({ num, icon: Icon, title, body }) => (
            <div key={num} className="flex flex-col items-center text-center sm:items-start sm:text-left">
              <div className="w-10 h-10 rounded-full bg-zinc-900 text-white flex items-center justify-center text-sm font-bold shrink-0">
                {num}
              </div>
              <p className="text-xl font-bold text-zinc-900 mt-4 mb-2">{title}</p>
              <p className="text-base text-zinc-600 leading-relaxed">{body}</p>
            </div>
          ))}
        </div>
      </div>
    </section>
  )
}

function FeatureBlocks() {
  return (
    <section className="bg-zinc-50 py-16 px-4">
      <div className="max-w-5xl mx-auto">
        <h2 className="text-3xl font-bold text-zinc-900 text-center mb-12">
          Everything your restaurant needs
        </h2>
        <div className="grid grid-cols-1 sm:grid-cols-2 gap-6">
          {features.map(({ icon: Icon, title, body }) => (
            <div
              key={title}
              className="bg-white rounded-2xl border border-zinc-200 p-6"
            >
              <div className="w-10 h-10 rounded-lg bg-zinc-100 flex items-center justify-center mb-4">
                <Icon className="w-5 h-5 text-zinc-700" />
              </div>
              <p className="text-xl font-bold text-zinc-900 mb-2">{title}</p>
              <p className="text-base text-zinc-600 leading-relaxed">{body}</p>
            </div>
          ))}
        </div>
      </div>
    </section>
  )
}

function FAQ() {
  return (
    <section className="bg-white py-16 px-4">
      <div className="max-w-2xl mx-auto">
        <h2 className="text-3xl font-bold text-zinc-900 text-center mb-10">
          Frequently asked questions
        </h2>
        <div className="divide-y divide-zinc-200">
          {faqs.map(({ q, a }) => (
            <details key={q} className="py-4 group">
              <summary className="text-base font-bold text-zinc-900 cursor-pointer list-none flex justify-between items-center min-h-[44px] focus:outline-none focus-visible:ring-2 focus-visible:ring-zinc-900">
                {q}
                <ChevronDown className="w-4 h-4 text-zinc-500 shrink-0 ml-4 group-open:rotate-180 transition-transform" />
              </summary>
              <p className="text-base text-zinc-600 leading-relaxed mt-3 pb-1">{a}</p>
            </details>
          ))}
        </div>
      </div>
    </section>
  )
}

function FooterCTABand() {
  return (
    <section className="bg-zinc-900 py-16 px-4">
      <div className="max-w-2xl mx-auto text-center">
        <h2 className="text-3xl font-bold text-white mb-4">Ready to get started?</h2>
        <p className="text-zinc-400 mb-8">Join the first restaurants using xmartmenu.</p>
        <a
          href="/auth/register"
          className="inline-flex items-center justify-center bg-white text-zinc-900 px-8 py-3.5 rounded-xl text-base font-bold hover:bg-zinc-100 transition-colors min-h-[44px] focus:outline-none focus:ring-2 focus:ring-white focus:ring-offset-2 focus:ring-offset-zinc-900"
        >
          Get started free
        </a>
      </div>
    </section>
  )
}

function Footer() {
  return (
    <footer className="bg-zinc-900 px-4 pb-8">
      <div className="max-w-5xl mx-auto border-t border-zinc-800 pt-8">
        <div className="flex flex-col sm:flex-row sm:justify-between gap-8">
          {/* Logo + tagline */}
          <div>
            <p className="text-white font-bold text-base">XmartMenu</p>
            <p className="text-zinc-400 text-sm mt-1">Cardápio digital para restaurantes</p>
            {/* Social icons */}
            <div className="flex gap-4 mt-4">
              <a
                href="#"
                aria-label="Instagram"
                className="text-zinc-400 hover:text-white transition-colors focus:outline-none focus:ring-2 focus:ring-zinc-400"
              >
                <Instagram className="w-5 h-5" />
              </a>
              <a
                href="#"
                aria-label="WhatsApp"
                className="text-zinc-400 hover:text-white transition-colors focus:outline-none focus:ring-2 focus:ring-zinc-400"
              >
                <MessageCircle className="w-5 h-5" />
              </a>
            </div>
          </div>
          {/* Legal links */}
          <div>
            <p className="text-sm font-bold text-zinc-400 uppercase tracking-wider mb-3">Legal</p>
            <ul className="flex flex-col gap-2">
              <li>
                <a href="/privacy" className="text-sm text-zinc-400 hover:text-white transition-colors">
                  Privacy Policy
                </a>
              </li>
              <li>
                <a href="/terms" className="text-sm text-zinc-400 hover:text-white transition-colors">
                  Terms of Service
                </a>
              </li>
            </ul>
          </div>
        </div>
        <p className="text-sm text-zinc-500 mt-8 text-center">
          © 2026 xmartmenu. All rights reserved.
        </p>
      </div>
    </footer>
  )
}

// ─── Page ─────────────────────────────────────────────────────────────────────

export default function LandingPage() {
  return (
    <>
      <Nav />
      <main>
        <Hero />
        <HowItWorks />
        <FeatureBlocks />
        <FAQ />
        <FooterCTABand />
      </main>
      <Footer />
    </>
  )
}
```

**Then update `src/app/page.tsx`** to re-export the marketing page. This makes Next.js App Router resolve `/` to the marketing page via the route group. Replace the entire file with:

```typescript
export { default } from './(marketing)/page'
```

NOTE: The `group-open:rotate-180` class on the ChevronDown icon works with Tailwind v4's `group` variant — when `<details>` has the `group` class and is in the `open` state, the sibling svg rotates. This is CSS-only; no JavaScript.
  </action>
  <verify>
    <automated>
      grep -n "export const dynamic = 'force-static'" /c/Users/Vanildo/Dev/xmartmenu/src/app/\(marketing\)/page.tsx
      grep -n "use client" /c/Users/Vanildo/Dev/xmartmenu/src/app/\(marketing\)/page.tsx
      grep -n "/auth/register" /c/Users/Vanildo/Dev/xmartmenu/src/app/\(marketing\)/page.tsx
      grep -c "details" /c/Users/Vanildo/Dev/xmartmenu/src/app/\(marketing\)/page.tsx
      grep -n "export { default }" /c/Users/Vanildo/Dev/xmartmenu/src/app/page.tsx
      npx tsc --noEmit --project /c/Users/Vanildo/Dev/xmartmenu/tsconfig.json 2>&1 | tail -5
    </automated>
  </verify>
  <acceptance_criteria>
    - src/app/(marketing)/page.tsx first line is `export const dynamic = 'force-static'`
    - src/app/(marketing)/page.tsx does NOT contain `'use client'`
    - src/app/(marketing)/page.tsx contains `href="/auth/register"` at least 2 times (nav + hero + footer CTA)
    - src/app/(marketing)/page.tsx contains `<details` exactly 6 times (one per FAQ question)
    - src/app/(marketing)/page.tsx contains `Your restaurant menu, online in minutes`
    - src/app/(marketing)/page.tsx contains `No credit card required.`
    - src/app/(marketing)/page.tsx contains `How It Works`
    - src/app/(marketing)/page.tsx contains `Everything your restaurant needs`
    - src/app/(marketing)/page.tsx contains `Frequently asked questions`
    - src/app/(marketing)/page.tsx contains `Ready to get started?`
    - src/app/(marketing)/page.tsx contains `href="/privacy"` and `href="/terms"`
    - src/app/(marketing)/page.tsx contains `aria-label="Instagram"` and `aria-label="WhatsApp"`
    - src/app/page.tsx contains `export { default } from './(marketing)/page'`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Full landing page rendered at /; all 7 sections present; force-static; no use client; all copy matches CONTEXT.md</done>
</task>

</tasks>

<verification>
Run after both tasks complete:

```bash
# TypeScript clean
npx tsc --noEmit

# force-static present
grep "force-static" src/app/(marketing)/page.tsx

# No use client
grep -rn "use client" src/app/\(marketing\)/

# CTA links correct
grep -c "/auth/register" src/app/(marketing)/page.tsx
# Must be >= 2

# FAQ has 6 details elements
grep -c "<details" src/app/(marketing)/page.tsx
# Must be 6

# Root passthrough in place
grep "export { default }" src/app/page.tsx

# All required copy strings present
grep "Your restaurant menu, online in minutes" src/app/(marketing)/page.tsx
grep "No credit card required." src/app/(marketing)/page.tsx
grep "How It Works" src/app/(marketing)/page.tsx
grep "Everything your restaurant needs" src/app/(marketing)/page.tsx
grep "Frequently asked questions" src/app/(marketing)/page.tsx
grep "Ready to get started?" src/app/(marketing)/page.tsx
grep "Privacy Policy" src/app/(marketing)/page.tsx
grep "Terms of Service" src/app/(marketing)/page.tsx
```
</verification>

<success_criteria>
- Visiting / renders the marketing landing page with all 7 sections (not a login redirect)
- All copy matches CONTEXT.md D-10 through D-21 exactly
- No use client anywhere in (marketing)/ files
- export const dynamic = 'force-static' is the first export in page.tsx
- FAQ accordion works with native details/summary (zero JavaScript)
- Privacy Policy links to /privacy, Terms of Service links to /terms
- npx tsc --noEmit exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/12-core-landing-page/12-02-SUMMARY.md`
</output>
