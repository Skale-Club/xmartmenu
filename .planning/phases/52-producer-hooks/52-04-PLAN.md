---
phase: 52-producer-hooks
plan: 04
type: execute
wave: 3
depends_on: ["52-01"]
files_modified:
  - scripts/xphere-queue-check.ts
  - package.json
autonomous: true
requirements: [FND-03]
must_haves:
  truths:
    - "An offline tsx gate asserts enqueueXphereSync is fail-open — a thrown publish is swallowed, no error escapes the call"
    - "The gate asserts the published body is the thin { tenantId, reason, eventId?, tags? } and deduplicationId === xphere:<tenantId>:<reason>"
    - "The gate asserts a silent no-op (zero publish calls) when QSTASH_TOKEN / XPHERE_WORKER_URL are unset"
    - "npm run xphere:check:queue runs the gate with a non-zero exit on any failed assertion, with NO real QStash credentials"
  artifacts:
    - path: "scripts/xphere-queue-check.ts"
      provides: "Offline fail-open + payload-shape assertion gate for enqueueXphereSync"
      contains: "deduplicationId"
    - path: "package.json"
      provides: "xphere:check:queue script"
      contains: "xphere:check:queue"
  key_links:
    - from: "scripts/xphere-queue-check.ts"
      to: "src/lib/xphere/queue.ts"
      via: "imports enqueueXphereSync and exercises it with a stubbed QStash Client"
      pattern: "enqueueXphereSync"
---

<objective>
Add an offline `tsx` assertion gate `scripts/xphere-queue-check.ts` (+ `npm run xphere:check:queue`) that proves the Phase 52 producer is fail-open and builds the correct message — mirroring the existing `scripts/xphere-client-check.ts` convention (no test runner introduced; `node:assert/strict`, non-zero exit on failure, no real credentials).

Purpose: lock the FND-03 invariants behind a runnable gate — (1) enqueue swallows a thrown publish and never throws into the caller, (2) the published body is the thin `{ tenantId, reason, eventId?, tags? }` with `deduplicationId: xphere:<tenantId>:<reason>`, (3) a silent no-op when env is unset.
Output: `scripts/xphere-queue-check.ts` + a package.json script.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-producer-hooks/52-CONTEXT.md

<interfaces>
Target under test (from 52-01):
```typescript
// src/lib/xphere/queue.ts
export async function enqueueXphereSync(
  tenantId: string,
  reason: SyncReason,
  opts?: { eventId?: string; tags?: string[] },
): Promise<void>
```
Env gate: no-op when `QSTASH_TOKEN` is unset OR neither `XPHERE_WORKER_URL` nor `NEXT_PUBLIC_APP_URL` is set.
Publish: `client.publishJSON({ url, body: { tenantId, reason, eventId?, tags? }, retries: 5, deduplicationId: 'xphere:<tenantId>:<reason>' })`.

The Client comes from `@upstash/qstash`. The gate must run with NO real QStash account. To intercept publishJSON without real creds, stub at the network boundary the SDK uses (the Client issues an HTTP request via fetch). Mirror the fetch-stub approach in scripts/xphere-client-check.ts (it replaces globalThis.fetch and records calls). Set QSTASH_TOKEN + XPHERE_WORKER_URL to dummy values in-process so the module-level `client` is constructed, then stub fetch.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write scripts/xphere-queue-check.ts and register npm run xphere:check:queue</name>
  <files>scripts/xphere-queue-check.ts, package.json</files>
  <read_first>
    - scripts/xphere-client-check.ts (the convention to mirror EXACTLY: node:assert/strict; setEnv reset helper; stubFetch replacing globalThis.fetch and recording calls; main() async + .catch(process.exit(1)); restore originalFetch; final console.log success line)
    - src/lib/xphere/queue.ts (the function under test — confirm env-gate vars, the exact body fields, deduplicationId format, retries)
    - package.json (the scripts block — add the new script next to xphere:worker:check)
  </read_first>
  <action>
    Create `scripts/xphere-queue-check.ts`. Code + comments in English. Mirror scripts/xphere-client-check.ts structure (header comment, helpers, `main().catch(...process.exit(1))`).

    IMPORTANT — module-load-time env: `src/lib/xphere/queue.ts` reads `QSTASH_TOKEN` at MODULE LOAD to construct `client`. So the script MUST set `process.env.QSTASH_TOKEN` and `process.env.XPHERE_WORKER_URL` to dummy values BEFORE importing queue.ts, OR import queue.ts dynamically AFTER setting env. Use a dynamic import inside main() after setting env so the module-level client is constructed with the dummy token:
    ```typescript
    process.env.QSTASH_TOKEN = 'dummy-token-not-real'
    process.env.XPHERE_WORKER_URL = 'https://example.test/api/internal/xphere-sync'
    const { enqueueXphereSync } = await import('@/lib/xphere/queue')
    ```
    (If a static top-level import is used instead, set the env vars at the very top of the file BEFORE the import line — but dynamic import after setEnv is cleaner and matches the intent. Either is acceptable as long as the client is constructed with the dummy token.)

    Stub the network the SDK uses (the QStash Client publishes via fetch). Reuse the stubFetch helper shape from xphere-client-check.ts: replace `globalThis.fetch`, record `{ url, init }`, and from `init.body` (a JSON string) parse the published payload.

    Assertions (use `node:assert/strict`):

    1. **Success path — correct message shape.** With dummy creds set and a fetch stub returning a 200:
       - call `await enqueueXphereSync('tenant-uuid-1', 'plan_changed', { eventId: 'evt_123', tags: ['upgrade'] })`
       - assert exactly one fetch call was made
       - parse the request body JSON; assert it deep-equals (or contains) `{ tenantId: 'tenant-uuid-1', reason: 'plan_changed', eventId: 'evt_123', tags: ['upgrade'] }`
       - assert the published payload's `deduplicationId === 'xphere:tenant-uuid-1:plan_changed'`
       - assert `retries === 5`
       NOTE: the QStash Client wraps publishJSON into its own HTTP request — the deduplicationId/retries may be carried in headers (e.g. `Upstash-Deduplication-Id`, `Upstash-Retries`) rather than the JSON body, and the JSON body may be exactly the `{ tenantId, reason, eventId, tags }`. INSPECT what the stubbed fetch actually receives (log `calls[0].url` and `calls[0].init`) and assert against the real shape: the message JSON body carries the thin payload, and the dedup id + retries appear in the request (body or headers). Assert on wherever they actually land. The load-bearing facts are: (a) the thin payload fields are present, (b) the dedup id string equals `xphere:tenant-uuid-1:plan_changed`, (c) retries is 5.

    2. **Fail-open — thrown publish is swallowed.** Replace the fetch stub with one that throws (e.g. `throw new TypeError('qstash down')`). Assert that `await enqueueXphereSync('t2', 'onboarded')` RESOLVES (does not reject) — wrap in try/catch and `assert.fail` if it throws. This is the core FND-03 invariant.

    3. **Silent no-op when env unset.** Delete `QSTASH_TOKEN` is not enough (the module client was already built at import). Instead assert the URL-gate no-op: delete `XPHERE_WORKER_URL` and `NEXT_PUBLIC_APP_URL`, install a fetch stub that throws if called, and assert `await enqueueXphereSync('t3', 'manual')` makes ZERO fetch calls and does not throw. (This proves the `if (!url) return` no-op branch.) Document in a comment that the token-unset no-op is covered by the rate-limit.ts-style `!client` guard which is exercised at module load.

    4. Restore `globalThis.fetch` to the original in a finally/at the end, and print `console.log('xphere-queue-check: all assertions passed')` on success.

    Then register the script in `package.json` scripts (next to `"xphere:worker:check"`):
    ```json
    "xphere:check:queue": "npx tsx scripts/xphere-queue-check.ts"
    ```

    Rules:
    - No real QStash/Xphere credentials — dummy in-process env only.
    - No vitest / test runner — `node:assert/strict` + tsx, matching the existing scripts.
    - The gate must exit non-zero on any failed assertion (the `main().catch(err => { console.error(err); process.exit(1) })` wrapper).
  </action>
  <verify>
    <automated>npm run xphere:check:queue</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/xphere-queue-check.ts` exists
    - `grep -q "enqueueXphereSync" scripts/xphere-queue-check.ts` succeeds
    - `grep -q "deduplicationId\|Deduplication" scripts/xphere-queue-check.ts` succeeds (dedup id asserted, in body or header)
    - `grep -q "xphere:tenant-uuid-1:plan_changed" scripts/xphere-queue-check.ts` succeeds (exact dedup id value asserted)
    - `grep -q "node:assert/strict" scripts/xphere-queue-check.ts` succeeds
    - `grep -q "all assertions passed" scripts/xphere-queue-check.ts` succeeds
    - `grep -q "xphere:check:queue" package.json` succeeds
    - `npm run xphere:check:queue` exits 0 and prints the success line
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    `npm run xphere:check:queue` passes offline with no real credentials, asserting: fail-open (thrown publish swallowed, no throw escapes), correct thin message shape + `deduplicationId: xphere:<tenantId>:<reason>` + `retries: 5`, and a zero-fetch no-op when the publish URL is unresolvable. `tsc --noEmit` clean.
  </done>
</task>

</tasks>

<verification>
- `npm run xphere:check:queue` exits 0.
- `npx tsc --noEmit` clean.
- No real QStash credentials used; assertions cover fail-open + payload shape + no-op.
</verification>

<success_criteria>
- FND-03 proven by a runnable offline gate: enqueueXphereSync is fail-open (never throws), builds the correct `{ tenantId, reason, deduplicationId }` message, and silently no-ops when env is unset.
</success_criteria>

<output>
After completion, create `.planning/phases/52-producer-hooks/52-04-SUMMARY.md`.
</output>
