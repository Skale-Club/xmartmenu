---
phase: 27-filter-chips-sound
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(admin)/orders/OrdersClient.tsx
autonomous: true
requirements: [KDS-10, KDS-11, KDS-12, KDS-13]

must_haves:
  truths:
    - "KDS header shows four chips (Pendentes, Em preparo, Prontos, Todos) plus Ativos as default; only pending+preparing cards visible by default"
    - "Clicking a chip immediately filters visible cards without any server refetch"
    - "Active filter chip survives a page reload via localStorage key kds_filter_{tenantId}"
    - "A new order arriving via Realtime INSERT triggers an audible beep unless muted"
    - "The beep does NOT play for status-change UPDATE events, only on INSERT"
    - "Mute/unmute button in KDS header shows Bell (unmuted) or BellOff (muted) from lucide-react"
    - "Muted state survives a page reload via localStorage key kds_mute_{tenantId}"
  artifacts:
    - path: "src/app/(admin)/orders/OrdersClient.tsx"
      provides: "Filter chips UI, localStorage persistence for filter and mute, playBeep function, muted state, Bell/BellOff toggle"
      contains: "kds_filter_"
  key_links:
    - from: "activeFilter state"
      to: "visibleOrders derived array"
      via: "local Array.filter applied on orders state, no refetch"
      pattern: "visibleOrders"
    - from: "Realtime INSERT handler inside if(fullOrder) block"
      to: "playBeep()"
      via: "conditional call guarded by !muted"
      pattern: "playBeep"
    - from: "toggleMute helper"
      to: "localStorage"
      via: "kds_mute_{tenantId} key"
      pattern: "kds_mute_"
---

<objective>
Add four status filter chips, localStorage persistence for filter and mute, a Web Audio API beep on Realtime INSERT orders, and a Bell/BellOff mute toggle. All changes are additive within the single existing file OrdersClient.tsx.

Purpose: Kitchen staff can focus on actionable orders and are alerted the instant a new order arrives.
Output: Modified OrdersClient.tsx with filter chips above the grid/list toggle, a mute button in the header row, and a beep wired exclusively to the Realtime INSERT handler.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

The only file to modify is src/app/(admin)/orders/OrdersClient.tsx. Read it completely before making any changes.

Key facts from the current file:

- Line 7: lucide-react imports are `{ LayoutGrid, List, MessageSquare }` - extend this to add Bell and BellOff
- Line 38: `const KDS_VIEW_KEY = (tenantId: string) => \`kds_view_${tenantId}\`` - pattern to follow for new keys
- Lines 163-166: SSR-safe localStorage useEffect for view - follow same pattern for filter and mute
- Lines 169-201: Realtime INSERT handler. The beep goes INSIDE the `if (fullOrder) { ... }` block, AFTER the setOrders call. There is no PATCH/UPDATE subscription - do not add playBeep anywhere else.
- Lines 241-261: Header JSX. Chips row goes between the closing header div and the orders content. Mute button goes inside the existing flex gap-3 container as the first child before the orders count paragraph.
- The orders state variable is named `orders`. The derived filtered list is named `visibleOrders`. Replace all rendering references from `orders.map` and `orders.length` with `visibleOrders.map` and `visibleOrders.length`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Filter chips + mute button + playBeep in OrdersClient.tsx</name>

  <read_first>
    src/app/(admin)/orders/OrdersClient.tsx
  </read_first>

  <files>src/app/(admin)/orders/OrdersClient.tsx</files>

  <action>
Make the following additive changes to OrdersClient.tsx. Do not remove any existing code.

CHANGE 1 - Extend lucide-react import (line 7):
  FROM: import { LayoutGrid, List, MessageSquare } from 'lucide-react'
  TO:   import { LayoutGrid, List, MessageSquare, Bell, BellOff } from 'lucide-react'

CHANGE 2 - Add key helpers after KDS_VIEW_KEY (after line 38):
  type FilterValue = 'active' | 'pending' | 'preparing' | 'ready' | 'all'
  const KDS_FILTER_KEY = (tenantId: string) => `kds_filter_${tenantId}`
  const KDS_MUTE_KEY   = (tenantId: string) => `kds_mute_${tenantId}`

CHANGE 3 - Add playBeep function at module level (after KDS_MUTE_KEY, before OrderCard component):
  function playBeep() {
    try {
      const ctx = new AudioContext()
      const osc = ctx.createOscillator()
      const gain = ctx.createGain()
      osc.connect(gain)
      gain.connect(ctx.destination)
      osc.type = 'sine'
      osc.frequency.value = 880
      gain.gain.setValueAtTime(0.3, ctx.currentTime)
      gain.gain.exponentialRampToValueAtTime(0.001, ctx.currentTime + 0.3)
      osc.start(ctx.currentTime)
      osc.stop(ctx.currentTime + 0.3)
    } catch { /* AudioContext blocked or not supported */ }
  }

CHANGE 4 - Add state inside OrdersClient (after the existing `const [view, setView]` line):
  const [activeFilter, setActiveFilter] = useState<FilterValue>('active')
  const [muted, setMuted] = useState(false)

CHANGE 5 - Add useEffect for filter+mute restore (after the existing view-restore useEffect):
  useEffect(() => {
    const savedFilter = localStorage.getItem(KDS_FILTER_KEY(tenantId))
    if (savedFilter) setActiveFilter(savedFilter as FilterValue)
    const savedMute = localStorage.getItem(KDS_MUTE_KEY(tenantId))
    if (savedMute === 'true') setMuted(true)
  }, [tenantId])

CHANGE 6 - Add setFilter and toggleMute helpers (after the existing `toggleView` function):
  function setFilter(next: FilterValue) {
    setActiveFilter(next)
    localStorage.setItem(KDS_FILTER_KEY(tenantId), next)
  }

  function toggleMute() {
    setMuted((prev) => {
      const next = !prev
      localStorage.setItem(KDS_MUTE_KEY(tenantId), String(next))
      return next
    })
  }

CHANGE 7 - Wire playBeep to Realtime INSERT handler.
  Inside the `if (fullOrder) { ... }` block, AFTER the `setOrders(...)` call, add one line:
    if (!muted) playBeep()

  The resulting block must look like:
    if (fullOrder) {
      setOrders((prev) => {
        if (prev.some((o) => o.id === fullOrder.id)) return prev
        return [fullOrder as OrderWithItems, ...prev]
      })
      if (!muted) playBeep()
    }

  CRITICAL: playBeep() is called in this ONE place only.
  Do NOT add it to the polling interval callback or anywhere else.

CHANGE 8 - Add visibleOrders derived value before the return statement:
  const visibleOrders = activeFilter === 'all'
    ? orders
    : activeFilter === 'active'
    ? orders.filter((o) => o.status === 'pending' || o.status === 'preparing')
    : orders.filter((o) => o.status === activeFilter)

CHANGE 9 - Add CHIPS array before the return statement (after visibleOrders):
  const CHIPS: { id: FilterValue; label: string }[] = [
    { id: 'active',    label: 'Ativos' },
    { id: 'pending',   label: 'Pendentes' },
    { id: 'preparing', label: 'Em preparo' },
    { id: 'ready',     label: 'Prontos' },
    { id: 'all',       label: 'Todos' },
  ]

CHANGE 10 - Update header JSX with mute button and chips row.

  A. Add mute button as first child of the flex gap-3 container in the header
     (the div containing the orders count paragraph and the grid/list toggle):

     {/* Mute toggle - KDS-13 */}
     <button
       onClick={toggleMute}
       aria-label={muted ? 'Ativar som' : 'Silenciar'}
       className={muted ? 'text-zinc-600 hover:text-zinc-400' : 'text-zinc-400 hover:text-zinc-200'}
     >
       {muted ? <BellOff size={24} /> : <Bell size={24} />}
     </button>

  B. Insert chips row AFTER the closing `</div>` of the header flex row and BEFORE
     the orders content (the `{visibleOrders.length === 0 ? ...` block):

     {/* Filter chips - KDS-10 */}
     <div className="flex items-center gap-2 mb-4 flex-wrap">
       {CHIPS.map((chip) => (
         <button
           key={chip.id}
           onClick={() => setFilter(chip.id)}
           className={
             activeFilter === chip.id
               ? 'rounded-full px-3 py-1 text-sm font-medium bg-zinc-700 text-white'
               : 'rounded-full px-3 py-1 text-sm font-medium bg-zinc-800 text-zinc-400 hover:bg-zinc-700 hover:text-white'
           }
         >
           {chip.label}
         </button>
       ))}
     </div>

  C. Replace ALL rendering references to orders with visibleOrders:
     - The count paragraph: {visibleOrders.length} pedido(s)
     - The empty-state condition: {visibleOrders.length === 0 ? ...
     - The grid map: {visibleOrders.map((order) => ...
     - The table tbody map: {visibleOrders.map((order) => ...
     The `orders` state variable itself is kept — visibleOrders is a derived view only.
  </action>

  <verify>
    <automated>cd C:/Users/Vanildo/Dev/xmartmenu && npx tsc --noEmit && node -e "const fs=require('fs');const c=fs.readFileSync('src/app/(admin)/orders/OrdersClient.tsx','utf8');const checks=['kds_filter_','kds_mute_','playBeep','Bell','BellOff','visibleOrders','activeFilter','toggleMute','AudioContext','setFilter'];const missing=checks.filter(k=>!c.includes(k));if(missing.length)throw new Error('MISSING: '+missing.join(', '));console.log('All checks passed')"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "kds_filter_" src/app/(admin)/orders/OrdersClient.tsx` returns 3 or more (const declaration + localStorage read in useEffect + localStorage.setItem in setFilter)
    - `grep -c "kds_mute_" src/app/(admin)/orders/OrdersClient.tsx` returns 3 or more (const declaration + localStorage read in useEffect + localStorage.setItem in toggleMute)
    - `grep -n "playBeep" src/app/(admin)/orders/OrdersClient.tsx` returns exactly 2 lines: the function declaration and the call inside the INSERT handler
    - `grep -n "BellOff" src/app/(admin)/orders/OrdersClient.tsx` returns hits in both the import line and the JSX conditional
    - `grep -c "visibleOrders" src/app/(admin)/orders/OrdersClient.tsx` returns 4 or more (declaration + empty-state + grid map + table map)
    - `grep -c "AudioContext" src/app/(admin)/orders/OrdersClient.tsx` returns exactly 1 (inside playBeep only)
    - `grep -A 5 "setInterval" src/app/(admin)/orders/OrdersClient.tsx` does NOT contain "playBeep" (beep not in polling)
    - `grep "bg-zinc-700 text-white" src/app/(admin)/orders/OrdersClient.tsx` returns at least 1 line (active chip class)
    - `grep "bg-zinc-800 text-zinc-400" src/app/(admin)/orders/OrdersClient.tsx` returns at least 1 line (inactive chip class)
    - `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>

  <done>
    OrdersClient.tsx compiles with zero TypeScript errors and contains all four features:
    (1) filter chips with activeFilter='active' default, filtering visibleOrders locally with no server calls;
    (2) kds_filter_{tenantId} read on mount and written on setFilter;
    (3) playBeep() called only in the Realtime INSERT handler guarded by !muted, with AudioContext in try/catch;
    (4) Bell/BellOff mute button in header, kds_mute_{tenantId} read on mount and written on toggleMute.
  </done>
</task>

</tasks>

<verification>
After the task completes, run these checks:

1. TypeScript clean:
   npx tsc --noEmit

2. playBeep appears exactly twice (declaration + one INSERT-handler call):
   grep -n "playBeep" src/app/(admin)/orders/OrdersClient.tsx

3. playBeep NOT in polling interval:
   grep -A 10 "setInterval" src/app/(admin)/orders/OrdersClient.tsx | grep "playBeep"
   (must return nothing)

4. Active chip Tailwind literal present:
   grep "bg-zinc-700 text-white" src/app/(admin)/orders/OrdersClient.tsx

5. Inactive chip Tailwind literal present:
   grep "bg-zinc-800 text-zinc-400" src/app/(admin)/orders/OrdersClient.tsx

6. Bell and BellOff imported and used:
   grep "Bell\|BellOff" src/app/(admin)/orders/OrdersClient.tsx
</verification>

<success_criteria>
1. KDS header shows chips (Ativos default, Pendentes, Em preparo, Prontos, Todos) filtering cards locally.
2. Active filter persists across page reload via localStorage key kds_filter_{tenantId}.
3. A new order via Realtime INSERT plays an 880 Hz sine beep (0.3 s) unless muted; status-change events are silent.
4. Bell/BellOff button in the header reflects and toggles mute state; state persists via kds_mute_{tenantId}.
5. npx tsc --noEmit exits 0.
</success_criteria>

<output>
After completion, create `.planning/phases/27-filter-chips-sound/27-01-SUMMARY.md` following the summary template.
</output>
