---
phase: 15-database-indices
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/15-database-indices/15-01-explain-queries.sql
  - .planning/phases/15-database-indices/15-01-explain-results.md
autonomous: false
requirements:
  - DB-01
must_haves:
  truths:
    - "EXPLAIN ANALYZE output for public menu query path is captured and documented"
    - "Seq Scan vs Index Scan status is known for menus, categories, and products tables on the menu_id column"
    - "Seq Scan vs Index Scan status is known for menus table on the slug column"
  artifacts:
    - path: ".planning/phases/15-database-indices/15-01-explain-queries.sql"
      provides: "Copy-pasteable EXPLAIN ANALYZE SQL for all public menu queries"
      contains: "EXPLAIN ANALYZE"
    - path: ".planning/phases/15-database-indices/15-01-explain-results.md"
      provides: "Raw EXPLAIN ANALYZE output for all public menu query paths"
      contains: "Seq Scan"
  key_links:
    - from: "/{slug}/{menuSlug} page.tsx"
      to: "categories(menu_id), products(menu_id)"
      via: ".eq('menu_id', menu.id)"
      pattern: "categories\\.menu_id|products\\.menu_id"
    - from: "/{slug} page.tsx"
      to: "menus(tenant_id)"
      via: ".eq('tenant_id', tenant.id)"
      pattern: "menus\\.tenant_id"
---

<objective>
Run EXPLAIN ANALYZE on every query executed by the two public menu routes, capture the planner output, and identify which tables use Seq Scan versus Index Scan.

Purpose: The public menu route (/{slug}/{menuSlug}) has LCP 3.0s and is the highest-priority DB path. Before writing any index migration we need the query planner's actual execution plan to know which tables are missing indices.

Output: A SQL file with copy-pasteable EXPLAIN ANALYZE statements, and a markdown file documenting the results.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-instrumentacao/14-BASELINE.md

<interfaces>
<!-- Query paths extracted from source files. Executor uses these directly. -->

From src/app/(public)/[slug]/[menuSlug]/page.tsx — queries executed on every page load:
1. SELECT * FROM tenants WHERE slug = $1 AND is_active = true         (getTenantBySlug)
2. SELECT * FROM tenants JOIN tenant_settings ON tenant_id             (same call, with embedded join)
3. SELECT * FROM menus WHERE slug = $menuSlug AND is_active = true     (menu by slug lookup)
4. SELECT * FROM categories WHERE menu_id = $menuId AND is_active = true ORDER BY position
5. SELECT * FROM products WHERE menu_id = $menuId AND is_available = true ORDER BY position
6. SELECT * FROM product_option_groups WHERE product_id IN (...) ORDER BY position  (conditional)

From src/app/(public)/[slug]/page.tsx — queries for tenant slug landing:
1. SELECT * FROM tenants WHERE slug = $1 AND is_active = true         (getTenantBySlug)
2. SELECT * FROM menus WHERE tenant_id = $1 AND is_active = true AND is_default = true  (default menu)
3. SELECT * FROM menus WHERE tenant_id = $1 AND is_active = true ORDER BY position LIMIT 1  (fallback)
4. SELECT * FROM categories WHERE menu_id = $menuId AND is_active = true ORDER BY position
5. SELECT * FROM products WHERE menu_id = $menuId AND is_available = true ORDER BY position

Existing indices confirmed in migrations 001 and 019:
- idx_tenants_slug       ON tenants(slug)
- idx_products_tenant    ON products(tenant_id)
- idx_products_category  ON products(category_id)
- idx_categories_tenant  ON categories(tenant_id)
- idx_orders_tenant      ON orders(tenant_id)
- idx_orders_created_at  ON orders(created_at)
- idx_order_items_order  ON order_items(order_id)
- idx_option_groups_product ON product_option_groups(product_id)   [migration 021]
- idx_option_groups_tenant  ON product_option_groups(tenant_id)   [migration 021]
- idx_options_group      ON product_options(group_id)             [migration 021]
- idx_options_tenant     ON product_options(tenant_id)            [migration 021]
- idx_scan_events_tenant ON scan_events(tenant_id)

UNIQUE constraint on menus(tenant_id, slug) — creates a composite unique index.
UNIQUE constraint on tenant_settings(tenant_id) — creates an implicit single-column index.

Likely MISSING (no single-column index seen in any migration):
- menus(tenant_id)      — queried alone without slug in /{slug} default menu lookup
- menus(slug)           — queried alone in /{slug}/{menuSlug} route; composite unique index on (tenant_id, slug) does NOT efficiently serve slug-only lookups
- categories(menu_id)   — filtered by menu_id in both routes; no index exists
- products(menu_id)     — filtered by menu_id in both routes; no index exists
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write EXPLAIN ANALYZE SQL queries for public menu paths</name>
  <files>.planning/phases/15-database-indices/15-01-explain-queries.sql</files>
  <read_first>
    - src/app/(public)/[slug]/[menuSlug]/page.tsx
    - src/app/(public)/[slug]/page.tsx
    - supabase/migrations/001_initial_schema.sql
    - supabase/migrations/019_full_schema_sync.sql
  </read_first>
  <action>
Create the file `.planning/phases/15-database-indices/15-01-explain-queries.sql` containing EXPLAIN ANALYZE statements for every distinct query executed by the two public menu routes.

Use the ACTUAL column names from the schema (confirmed in migrations). Replace UUIDs with realistic placeholder UUIDs (gen_random_uuid() output format: xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx). Use a slug value that matches the demo tenant (slug = 'restaurante-teste' as seen in 14-BASELINE.md).

The file must contain exactly these labelled sections:

-- Phase 15 Plan 01 — EXPLAIN ANALYZE for public menu query paths
-- Run each section independently in Supabase SQL Editor
-- Replace placeholder UUIDs with real IDs from your database before running
-- How to find real IDs: SELECT id, slug FROM tenants LIMIT 5;
--                       SELECT id, tenant_id, slug FROM menus LIMIT 5;
--                       SELECT id, menu_id FROM categories LIMIT 5;

--- SECTION A: /{slug} tenant lookup ---
EXPLAIN ANALYZE
SELECT t.*, ts.*
FROM tenants t
LEFT JOIN tenant_settings ts ON ts.tenant_id = t.id
WHERE t.slug = 'restaurante-teste' AND t.is_active = true;

--- SECTION B: /{slug} default menu lookup ---
EXPLAIN ANALYZE
SELECT *
FROM menus
WHERE tenant_id = '00000000-0000-0000-0000-000000000001'
  AND is_active = true
  AND is_default = true;

--- SECTION C: /{slug} fallback menu lookup ---
EXPLAIN ANALYZE
SELECT *
FROM menus
WHERE tenant_id = '00000000-0000-0000-0000-000000000001'
  AND is_active = true
ORDER BY position
LIMIT 1;

--- SECTION D: /{slug}/{menuSlug} menu by slug lookup ---
EXPLAIN ANALYZE
SELECT *
FROM menus
WHERE slug = 'main'
  AND is_active = true;

--- SECTION E: categories by menu_id ---
EXPLAIN ANALYZE
SELECT *
FROM categories
WHERE menu_id = '00000000-0000-0000-0000-000000000002'
  AND is_active = true
ORDER BY position;

--- SECTION F: products by menu_id ---
EXPLAIN ANALYZE
SELECT *
FROM products
WHERE menu_id = '00000000-0000-0000-0000-000000000002'
  AND is_available = true
ORDER BY position;

--- SECTION G: product_option_groups by product_id IN (list) ---
EXPLAIN ANALYZE
SELECT *
FROM product_option_groups
WHERE product_id IN (
  '00000000-0000-0000-0000-000000000003',
  '00000000-0000-0000-0000-000000000004'
)
ORDER BY position;
  </action>
  <verify>
    <automated>Test-Path "C:/Users/Vanildo/Dev/xmartmenu/.planning/phases/15-database-indices/15-01-explain-queries.sql"</automated>
  </verify>
  <done>File exists with 7 labelled EXPLAIN ANALYZE sections (A through G), placeholder UUIDs, and a usage comment at the top.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human runs EXPLAIN ANALYZE in Supabase SQL Editor and records results</name>
  <files>.planning/phases/15-database-indices/15-01-explain-results.md</files>
  <action>
This checkpoint pauses execution. The human runs the 7 EXPLAIN ANALYZE queries from Task 1 in Supabase SQL Editor, then provides the output. After receiving the human's response, create `.planning/phases/15-database-indices/15-01-explain-results.md` documenting all findings.

Before running, the human needs real UUIDs. Provide this helper query:

```sql
-- Step 0: Get real IDs to substitute into the EXPLAIN queries
SELECT 'tenant' as type, id, slug FROM tenants LIMIT 3
UNION ALL
SELECT 'menu' as type, id, slug FROM menus LIMIT 3
UNION ALL
SELECT 'category' as type, id, menu_id::text FROM categories LIMIT 3
UNION ALL
SELECT 'product' as type, id, menu_id::text FROM products LIMIT 3;
```

Instructions for the human:
1. Open Supabase Dashboard → SQL Editor
2. Run the Step 0 helper query to collect real UUIDs
3. For each section A through G in `.planning/phases/15-database-indices/15-01-explain-queries.sql`, substitute real UUIDs and run the EXPLAIN ANALYZE
4. Look for "Seq Scan" vs "Index Scan" in each output
5. Report findings back

Expected findings based on migration analysis:
- Section A (tenants.slug): Index Scan using idx_tenants_slug — PASS
- Section B & C (menus.tenant_id): likely Seq Scan — no menus(tenant_id) index exists
- Section D (menus.slug): likely Seq Scan — composite unique(tenant_id, slug) may not serve slug-only lookups
- Section E (categories.menu_id): likely Seq Scan — no categories(menu_id) index exists
- Section F (products.menu_id): likely Seq Scan — no products(menu_id) index exists
- Section G (product_option_groups.product_id): Index Scan using idx_option_groups_product — PASS

After receiving results, write `.planning/phases/15-database-indices/15-01-explain-results.md` with this structure:

# Phase 15 Plan 01 — EXPLAIN ANALYZE Results: Public Menu Path

**Date:** [today's date]
**DB:** Supabase (production)

## Summary Table

| Section | Query | Scan Type | Needs Index? |
|---------|-------|-----------|--------------|
| A | tenants WHERE slug = ... | [Index/Seq] Scan | [Yes/No] |
| B | menus WHERE tenant_id = ... AND is_default = true | [Index/Seq] Scan | [Yes/No] |
| C | menus WHERE tenant_id = ... ORDER BY position LIMIT 1 | [Index/Seq] Scan | [Yes/No] |
| D | menus WHERE slug = ... | [Index/Seq] Scan | [Yes/No] |
| E | categories WHERE menu_id = ... | [Index/Seq] Scan | [Yes/No] |
| F | products WHERE menu_id = ... | [Index/Seq] Scan | [Yes/No] |
| G | product_option_groups WHERE product_id IN (...) | [Index/Seq] Scan | [Yes/No] |

## Raw EXPLAIN ANALYZE Output

[Paste human's reported output for each section A through G]

## Indices Identified as Missing

[List only columns where Seq Scan was confirmed — these feed directly into Plan 03's migration]
  </action>
  <verify>
    <automated>Test-Path "C:/Users/Vanildo/Dev/xmartmenu/.planning/phases/15-database-indices/15-01-explain-results.md"</automated>
  </verify>
  <done>Results file exists with summary table filled in from human's output, raw EXPLAIN ANALYZE output recorded, and "Indices Identified as Missing" section populated with only the confirmed Seq Scan columns.</done>
</task>

</tasks>

<verification>
- `.planning/phases/15-database-indices/15-01-explain-queries.sql` exists with 7 labelled EXPLAIN ANALYZE sections
- `.planning/phases/15-database-indices/15-01-explain-results.md` exists with summary table and raw output
- Summary table identifies which tables use Seq Scan vs Index Scan for the public menu path
</verification>

<success_criteria>
EXPLAIN ANALYZE output is captured for all 7 queries in the public menu path, and the "Indices Identified as Missing" section is populated so Plan 03 can write the migration.
</success_criteria>

<output>
After completion, create `.planning/phases/15-database-indices/15-01-SUMMARY.md`
</output>
