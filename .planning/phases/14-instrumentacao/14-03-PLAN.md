---
phase: 14-instrumentacao
plan: 03
type: execute
wave: 2
depends_on:
  - 14-01
  - 14-02
files_modified:
  - .planning/phases/14-instrumentacao/14-BASELINE.md
autonomous: false
requirements:
  - PERF-01
  - FE-03
  - PERF-02
must_haves:
  truths:
    - "14-BASELINE.md exists with Lighthouse mobile scores for public menu and landing page"
    - "14-BASELINE.md contains bundle chunk table with top 5 chunks, KB sizes, and lazy-load assessments"
    - "14-BASELINE.md contains Supabase query timing for all measured paths"
    - "14-BASELINE.md is committed and readable by Phase 15 and 16 planners as the authoritative before state"
  artifacts:
    - path: ".planning/phases/14-instrumentacao/14-BASELINE.md"
      provides: "Pre-optimization baseline — scores/timings for all three deliverables"
      contains: "## Lighthouse Scores"
  key_links:
    - from: ".planning/phases/14-instrumentacao/14-BASELINE.md"
      to: "Phase 15 planner"
      via: "read_first in 15-01-PLAN.md"
      pattern: "14-BASELINE"
    - from: ".planning/phases/14-instrumentacao/14-BASELINE.md"
      to: "Phase 16 planner"
      via: "read_first in 16-01-PLAN.md"
      pattern: "14-BASELINE"
---

<objective>
Run PageSpeed Insights on the two priority routes, then write 14-BASELINE.md consolidating all Phase 14 measurement data: Lighthouse scores (PERF-01), bundle chunks (FE-03), and Supabase query timings (PERF-02).

Purpose: D-10/D-11 define the baseline file format and its role as the single source of truth for pre-optimization state. Phase 15 and 16 planners MUST read this file before planning.
Output: .planning/phases/14-instrumentacao/14-BASELINE.md committed to git.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/14-instrumentacao/14-CONTEXT.md
@.planning/phases/14-instrumentacao/14-01-SUMMARY.md
@.planning/phases/14-instrumentacao/14-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Run PageSpeed Insights on public menu and landing page</name>
  <files>.planning/phases/14-instrumentacao/14-BASELINE.md</files>
  <action>
    Human runs PageSpeed Insights at https://pagespeed.web.dev/ against production URLs.
    Per D-07: public menu first (priority), landing page second.
    Per D-08: Mobile tab is the binding metric.
    Per D-09: Use the deployed production URL (main branch).

    Fields to record per route:
    - Performance score (0-100)
    - LCP (seconds), CLS (dimensionless), INP (milliseconds)
    - FCP (seconds) and TBT (ms) — bonus context
    - Desktop Performance score

    Also check Vercel Speed Insights dashboard for real-traffic p75 CWV data (if no traffic, note explicitly).
  </action>

  <what-built>
    Plans 01 and 02 have produced bundle chunk data and Supabase query timings. This task collects the final missing measurement: Lighthouse scores via PageSpeed Insights.
  </what-built>

  <how-to-verify>
    1. Open https://pagespeed.web.dev/

    2. For Route 1 — Public menu (/{slug}/{menuSlug}):
       Enter production URL (e.g., https://xmartmenu.vercel.app/demo/cardapio)
       Click Analyze, wait ~30 seconds
       Select Mobile tab and record: Performance score, LCP, CLS, INP
       Select Desktop tab and record: Performance score

    3. For Route 2 — Landing page (/):
       Enter production root URL (e.g., https://xmartmenu.vercel.app/)
       Run analysis, record same fields

    4. Check Vercel Speed Insights:
       https://vercel.com/dashboard → project → Speed Insights tab
       If real-traffic p75 data shows per-route CWV, note those values
       If "Waiting for data", note "no real-traffic data"

    Provide all results to Claude in the resume signal below.
  </how-to-verify>

  <resume-signal>
    Type "PSI SCORES:" followed by values in this format:

    Route: /{slug}/{menuSlug}  (URL: https://...)
    Mobile Performance: NN | LCP: N.Ns | CLS: 0.NN | INP: NNNms | Desktop: NN

    Route: /  (URL: https://...)
    Mobile Performance: NN | LCP: N.Ns | CLS: 0.NN | INP: NNNms | Desktop: NN

    Speed Insights: [data or "no data yet"]

    Claude will then write 14-BASELINE.md from all three data sources.
  </resume-signal>

  <verify>
    <automated>MISSING — human measurement step, no automated check. Task 2 verify covers the resulting file.</automated>
  </verify>

  <done>PSI scores for both routes recorded and provided to Claude in resume signal.</done>
</task>

<task type="auto">
  <name>Task 2: Write 14-BASELINE.md with all measurements</name>
  <files>.planning/phases/14-instrumentacao/14-BASELINE.md</files>

  <read_first>
    - .planning/phases/14-instrumentacao/14-01-SUMMARY.md — bundle chunk data (names, KB, lazy-load assessments from Plan 01)
    - .planning/phases/14-instrumentacao/14-02-SUMMARY.md — Supabase query timings from Vercel logs (from Plan 02)
    - .planning/phases/14-instrumentacao/14-CONTEXT.md — D-11 baseline format specification
  </read_first>

  <action>
Create `.planning/phases/14-instrumentacao/14-BASELINE.md` using data from:
- PSI scores provided in Task 1 checkpoint resume signal
- Bundle chunk data in 14-01-SUMMARY.md
- Supabase timings in 14-02-SUMMARY.md

Structure the file with these four sections (D-11 format):

Section 1 — Header:
  Title: "# Phase 14 Baseline — Pre-Optimization Metrics"
  Fields: Captured date, production URL, git commit hash of main at measurement time
  Note: "These are the BEFORE values. Phase 15 and 16 improvements must beat these numbers."

Section 2 — "## Lighthouse Scores (PageSpeed Insights — Mobile)":
  Table columns: Route | URL | Mobile Perf | LCP | CLS | INP | Desktop Perf
  One row for public menu route, one row for landing page
  Measurement tool note: PageSpeed Insights synthetic Lighthouse, mobile emulation
  Vercel Speed Insights note: available or not available with reason
  Observations subsection: which route scored lower and is Phase 16 priority; any CWV outside thresholds (LCP > 2.5s is poor, CLS > 0.1 is poor, INP > 200ms is poor)

Section 3 — "## Bundle Analysis (@next/bundle-analyzer)":
  Run command and date
  Table columns: # | Chunk Name | Size (KB) | Type | Lazy-Load Candidate
  Top 5 chunks from 14-01-SUMMARY.md
  Type values: vendor (node_modules framework), page (route-specific), shared (cross-route component)
  Lazy-Load Candidate: "Yes — reason" or "No — reason"
  Observations subsection: total client JS if visible; highest-priority lazy-load candidates for Phase 16

Section 4 — "## Supabase Query Timing (console.time)":
  Measurement method note: Temporary console.time() probes, deployed and removed in Plan 02
  Table columns: Route | Query Label | Time (ms) | Notes
  Rows for all 7 labels from Plan 02 (or N/A with reason for any not triggered)
  Observations subsection: slowest query (candidate for Phase 15 index); any N/A entries explained

Section 5 — "## Phase 15 and 16 Targets":
  Phase 15 targets: all queries to show Index Scan in EXPLAIN ANALYZE; priority query named
  Phase 16 targets: both routes reach >= 90 Mobile Performance; current gap calculated; primary optimization lever named from bundle table

If any measurement value is genuinely missing (e.g., orders timing not triggerable), write "N/A — [specific reason]" rather than leaving a placeholder.

After writing, commit:

```bash
git add ".planning/phases/14-instrumentacao/14-BASELINE.md"
git commit -m "docs(14): record pre-optimization baseline (PERF-01, PERF-02, FE-03)"
```
  </action>

  <acceptance_criteria>
    - File `.planning/phases/14-instrumentacao/14-BASELINE.md` exists (verify: `Test-Path ".planning/phases/14-instrumentacao/14-BASELINE.md"`)
    - File contains "## Lighthouse Scores" header (verify: `Select-String -Path ".planning/phases/14-instrumentacao/14-BASELINE.md" -Pattern "Lighthouse Scores"`)
    - File contains "## Bundle Analysis" header (verify: `Select-String -Path ".planning/phases/14-instrumentacao/14-BASELINE.md" -Pattern "Bundle Analysis"`)
    - File contains "## Supabase Query Timing" header (verify: `Select-String -Path ".planning/phases/14-instrumentacao/14-BASELINE.md" -Pattern "Supabase Query Timing"`)
    - File contains "## Phase 15" (verify: `Select-String -Path ".planning/phases/14-instrumentacao/14-BASELINE.md" -Pattern "Phase 15"`)
    - No unresolved placeholder brackets remain (verify: `Select-String -Path ".planning/phases/14-instrumentacao/14-BASELINE.md" -Pattern "\[N\]|\[NN\]|\[name\]"` returns no matches)
    - Git log shows commit with message "docs(14): record pre-optimization baseline"
  </acceptance_criteria>

  <verify>
    <automated>Test-Path ".planning/phases/14-instrumentacao/14-BASELINE.md"</automated>
  </verify>

  <done>
    14-BASELINE.md written with all four sections populated with real measurement data, no placeholder values remaining, committed to git. Phases 15 and 16 planners can read this file to understand the before state.
  </done>
</task>

</tasks>

<verification>
- `Test-Path ".planning/phases/14-instrumentacao/14-BASELINE.md"` returns True
- All five sections present in file
- Lighthouse mobile scores for both routes recorded (numeric, not placeholders)
- Top 5 bundle chunks listed with lazy-load assessments
- Query timings from Plan 02 recorded (or marked N/A with explicit reason)
- File committed to git
</verification>

<success_criteria>
Phase 14 complete when all four success criteria from ROADMAP.md are satisfied:
1. PERF-01: LCP, CLS, INP readable by route — documented in 14-BASELINE.md Lighthouse section
2. PERF-02: Supabase query timing visible for 3 critical paths — documented in 14-BASELINE.md Query Timing section (probes removed from production)
3. FE-03: Bundle analysis with top chunks and lazy-loading notes — documented in 14-BASELINE.md Bundle Analysis section
4. 14-BASELINE.md committed and referenced by Phases 15 and 16
</success_criteria>

<output>
After completion, create `.planning/phases/14-instrumentacao/14-03-SUMMARY.md`
</output>
