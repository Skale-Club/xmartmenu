---
phase: 10-image-seeding
verified: 2026-05-07T00:00:00Z
status: human_needed
score: 13/13 must-haves verified (programmatic); pending human end-to-end
overrides_applied: 0
human_verification:
  - test: "Apply migration 023 in Supabase SQL editor"
    expected: "ai_jobs table exists with columns id, tenant_id, feature_key, status, created_at, completed_at, error_message and policy ai_jobs_superadmin"
    why_human: "Local Supabase Docker is not used in this project — migration must be applied manually in production Supabase SQL editor"
  - test: "Set GitHub repository secrets: GOOGLE_GENERATIVE_AI_API_KEY, NEXT_PUBLIC_SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, VERCEL_REVALIDATE_URL, VERCEL_REVALIDATE_SECRET"
    expected: "All 5 secrets visible in GitHub repo Settings -> Secrets and variables -> Actions"
    why_human: "GH repo secret presence cannot be verified by reading the codebase"
  - test: "Set Vercel env vars: GH_PAT (Fine-Grained PAT, not GITHUB_TOKEN), GITHUB_REPO_OWNER, GITHUB_REPO_NAME, VERCEL_REVALIDATE_SECRET"
    expected: "All 4 env vars set in Vercel project Production + Preview"
    why_human: "Vercel env var presence cannot be verified from the codebase"
  - test: "Trigger bulk image seeding (AI-07/AI-08): from superadmin tenant detail page, click 'Seed all images'"
    expected: "Polling status appears (~3s polls, 'Generating images via GH Actions...'); after a few minutes, success banner 'Images seeded. Refresh to see them.' appears; tenant_settings.banner_url is populated with the cover URL; products without image_url have image_url populated; cover.webp exists at tenant-assets/{tenantId}/cover.webp; product webp files exist at tenant-assets/{tenantId}/products/{productId}.webp"
    why_human: "Requires real Gemini API call, real GH Actions dispatch (HTTP 204), real Supabase storage upload, real polling round-trip, and visual confirmation of generated images"
  - test: "Trigger per-product image seeding (AI-09): click 'Seed image' on a product row"
    expected: "Job dispatches with productId set; only that product's image is generated; product.image_url is updated; cover photo is NOT regenerated (single-product mode skips cover); button text changes to 'Seeding...' during run"
    why_human: "Requires running the full GH Actions flow with single-product PRODUCT_ID input"
  - test: "Confirm products with existing image_url are skipped (D-09 additive)"
    expected: "Per-product list shows 'has image' label (no Seed button) for products that already have image_url; bulk re-runs do not regenerate existing covers (banner_url skip)"
    why_human: "Requires verifying skip-existing behavior end-to-end against a tenant that has some images and some without"
  - test: "Confirm public menu ISR cache is invalidated after seeding completes"
    expected: "Visiting /{tenantSlug} after a successful seed shows the new cover and product images without a manual refresh delay (revalidatePath fired by /api/revalidate POST and seed-status route)"
    why_human: "ISR revalidation effect can only be observed by visiting the public menu after a real GH Actions run"
  - test: "Confirm failure path: simulate Gemini API key missing or storage upload error"
    expected: "ai_jobs.status transitions to 'failed' with error_message populated; UI shows red banner with the error message; polling stops on failed"
    why_human: "Requires inducing a real failure in the GH Actions runtime"
  - test: "Confirm polling timeout: leave UI open during a job that exceeds ~5 min (100 polls)"
    expected: "After ~5 min, polling stops and UI shows 'Image seeding timed out. Check GH Actions logs.' (timeout guard fires)"
    why_human: "Requires real-time observation of timeout behavior"
  - test: "Confirm ai_usage telemetry recorded for image_seeding feature_key"
    expected: "After a successful seed, a row in ai_usage with feature_key='image_seeding', call_count=N (number of images generated), token_count=0 for today's date"
    why_human: "Requires checking the database after a real seed run"
---

# Phase 10: Image Seeding Verification Report

**Phase Goal:** Superadmin-only AI image seeding for tenant menus — generates cover photo (16:9) and per-product images (1:1) via Gemini 3.1 in GitHub Actions, uploads WebP to Supabase Storage, tracks via ai_jobs table with polling UI.
**Verified:** 2026-05-07
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

Sources merged: ROADMAP.md success criteria (3) + PLAN frontmatter must-haves (truths from 10-01, 10-02, 10-03 plans). Roadmap SCs are non-negotiable contracts.

| #   | Truth (source) | Status | Evidence |
| --- | -------------- | ------ | -------- |
| 1   | (Roadmap SC1) Superadmin triggers image seeding; cover photo generated by gemini-3.1-flash-image-preview is uploaded as banner_url | VERIFIED (programmatic) | scripts/seed-images.ts L44 `MODEL_NAME='gemini-3.1-flash-image-preview'`; L80 `aspectRatio: '16:9' \| '1:1'`; L221 `generateAndUploadImage(coverPrompt, coverPath, '16:9')`; L223-228 upserts `tenant_settings.banner_url` with public URL. Pending end-to-end (see human_verification #4) |
| 2   | (Roadmap SC2) Each product without image_url gets a generated AI photo uploaded to Supabase Storage | VERIFIED (programmatic) | scripts/seed-images.ts L244-245 `productsQuery.is('image_url', null)` (bulk filter); L295 `generateAndUploadImage(productPrompt, productPath, '1:1')`; L297-300 updates `products.image_url` with public URL; storage path `${TENANT_ID}/products/${product.id}.webp` (L294). Pending end-to-end (#4) |
| 3   | (Roadmap SC3) Superadmin can click "Seed image" button on a specific product row to generate just that product's image | VERIFIED (programmatic) | TenantDetailClient.tsx L716 `onClick={() => void handleSeedImages(product.id)}`; L720 `'Seed image'` button text; L241 `handleSeedImages(productId?)` POSTs `productId` to /seed-images; seed-images/route.ts L26 `featureKey = productId ? 'image_single' : 'image_seeding'`; scripts/seed-images.ts L246-247 single-product branch `productsQuery.eq('id', PRODUCT_ID)`. Pending end-to-end (#5) |
| 4   | (10-01) ai_jobs table exists with columns id, tenant_id, feature_key, status, created_at, completed_at, error_message | VERIFIED (DDL) | supabase/migrations/023_ai_jobs.sql L7-15 — exact columns and types match spec. **Pending application** in Supabase SQL editor (#1) |
| 5   | (10-01) AiJob TS type exported with status: 'pending' \| 'running' \| 'complete' \| 'failed' | VERIFIED | src/types/database.ts L167-175 — exact union type, completed_at and error_message nullable |
| 6   | (10-01) @google/genai installed and resolvable | VERIFIED | package.json L15 `"@google/genai": "^1.52.0"` |
| 7   | (10-01) POST /api/revalidate validates VERCEL_REVALIDATE_SECRET and calls revalidatePath() for tenant slug | VERIFIED | src/app/api/revalidate/route.ts L19 secret check; L27 `revalidatePath(/${tenantSlug})`; L28-30 optional menuSlug; runtime nodejs L8 |
| 8   | (10-02) GH Actions image-seeding.yml accepts job_id, tenant_id, product_id via workflow_dispatch | VERIFIED | .github/workflows/image-seeding.yml L4-15 workflow_dispatch with all 3 inputs; L20 `timeout-minutes: 15`; L33 `npx tsx scripts/seed-images.ts`; L38-42 all 5 secrets passed |
| 9   | (10-02) Script updates ai_jobs status pending -> running -> complete \| failed | VERIFIED | seed-images.ts L182 running; L334 complete; L351 failed (catch block); each `update().eq('id', JOB_ID)` |
| 10  | (10-02) Script generates cover (16:9) when product_id empty; product (1:1) for products without image_url | VERIFIED | L209 `if (isBulk)` cover branch; L221 cover with '16:9'; L295 product with '1:1'; L210-212 skip cover when banner_url already set (D-09) |
| 11  | (10-02) Script base64 -> Buffer -> sharp().webp({quality:85}) -> Supabase Storage with sequential 1500ms delay; sanitizeForPrompt on company_name + business_type; calls /api/revalidate after uploads | VERIFIED | L102 `Buffer.from(base64,'base64')`; L103 `sharp(pngBuffer).webp({quality:85}).toBuffer()`; L105-110 storage upload to `tenant-assets`; L25-31 inlined `sanitizeForPrompt`; L202-203 applied to tenant.name and businessType; L283-284 applied to product.name and categoryName; L47 RATE_LIMIT_DELAY_MS=1500; L234, L315 sleep calls; L329 `callRevalidate(tenant.slug)` |
| 12  | (10-03) POST /seed-images inserts ai_jobs row, dispatches workflow_dispatch (checks 204), returns { jobId } | VERIFIED | seed-images/route.ts L17 `assertSuperadmin()` 401 guard; L27-35 `from('ai_jobs').insert(...)`; L80 `dispatchRes.status !== 204` check; L89 `return NextResponse.json({ jobId })`; **does NOT call .json() on dispatchRes** (Pitfall 1); L63 `Bearer ${process.env.GH_PAT}`; L59 workflow filename `image-seeding.yml` |
| 13  | (10-03) GET /seed-status returns { status, errorMessage } from ai_jobs; UI polls every 3s, stops on complete/failed | VERIFIED | seed-status/route.ts L18 assertSuperadmin guard; L22 `searchParams.get('jobId')`; L29-34 `from('ai_jobs').select('status, error_message')` with `.eq('tenant_id', tenantId)` cross-tenant guard; L41-50 revalidatePath on complete; L53-56 returns `{ status, errorMessage }`. TenantDetailClient.tsx L265-305 setInterval at 3000ms; L268, L283, L292 clearInterval on timeout/complete/failed; L267 polls > 100 timeout guard |

**Score:** 13/13 truths verified programmatically. End-to-end behavior pending human verification.

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `supabase/migrations/023_ai_jobs.sql` | ai_jobs DDL + RLS policy | VERIFIED | 31 lines, contains `CREATE TABLE IF NOT EXISTS ai_jobs`, RLS enabled, `ai_jobs_superadmin` policy gated by profiles.role='superadmin' |
| `src/types/database.ts` | AiJob TS interface | VERIFIED | L167-175 exports `AiJob` with exact union type and nullable fields |
| `src/app/api/revalidate/route.ts` | Internal revalidate endpoint | VERIFIED, WIRED | 33 lines; `runtime='nodejs'`; secret check; `revalidatePath()` call. Wired from scripts/seed-images.ts L149 fetch |
| `package.json` | @google/genai dependency | VERIFIED | L15 `"@google/genai": "^1.52.0"`; sharp L27 `^0.34.5`; @supabase/supabase-js L17; tsx in devDeps L41 |
| `.github/workflows/image-seeding.yml` | GH Actions workflow_dispatch | VERIFIED | 43 lines; correct inputs, runner, secrets, tsx command |
| `scripts/seed-images.ts` | Image generation pipeline | VERIFIED, WIRED | 365 lines; complete pipeline; no `imageSize`, no `Promise.all`, no `@/` aliases (Pitfalls 4/5/8 avoided) |
| `src/app/api/superadmin/tenants/[id]/seed-images/route.ts` | POST trigger | VERIFIED, WIRED | 91 lines; assertSuperadmin guard; `runtime='nodejs'`; `maxDuration=15`; insert ai_jobs; `dispatchRes.status !== 204` check (no .json()); GH_PAT auth |
| `src/app/api/superadmin/tenants/[id]/seed-status/route.ts` | GET status poller | VERIFIED, WIRED | 58 lines; assertSuperadmin guard; tenant_id filter; revalidatePath on complete; returns `{status, errorMessage}` |
| `src/app/api/superadmin/tenants/[id]/products-list/route.ts` | Products list helper for AI-09 | VERIFIED, WIRED | 36 lines; assertSuperadmin guard; menuId filter; returns `{ products: [...] }`; consumed by TenantDetailClient L147 |
| `src/app/(superadmin)/tenants/[id]/TenantDetailClient.tsx` | Image seeding UI | VERIFIED, WIRED | 5 new state vars (L74-78); useEffect L145-151 fetches products; handleSeedImages L241-311; image seeding JSX L663-728; existing Phase 9 UI untouched |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| TenantDetailClient.handleSeedImages() | POST /api/superadmin/tenants/[id]/seed-images | fetch with `{ productId: '' \| id }` body | WIRED | TenantDetailClient.tsx L247-251 fetches with JSON body containing productId |
| TenantDetailClient polling setInterval | GET /api/superadmin/tenants/[id]/seed-status?jobId=... | setInterval every 3000ms | WIRED | L265 setInterval; L277-279 fetch with jobId in querystring; L268,283,292 clearInterval on timeout/complete/failed |
| seed-images/route.ts | GitHub API workflow_dispatch | fetch to `https://api.github.com/repos/${owner}/${repo}/actions/workflows/image-seeding.yml/dispatches` with Bearer GH_PAT | WIRED | L58-77 fetch with all required headers; L80 status !== 204 guard; L82-86 marks job failed on dispatch error |
| scripts/seed-images.ts | supabase ai_jobs table | `.from('ai_jobs').update({status:...}).eq('id', JOB_ID)` | WIRED | L182 running; L332-335 complete; L348-355 failed (catch) |
| scripts/seed-images.ts | Supabase Storage tenant-assets bucket | `.storage.from('tenant-assets').upload(path, webpBuffer, { contentType:'image/webp', upsert:true })` | WIRED | L105-110; paths `${TENANT_ID}/cover.webp` (L220) and `${TENANT_ID}/products/${product.id}.webp` (L294) |
| scripts/seed-images.ts | /api/revalidate | fetch POST with `{ secret, tenantSlug }` body | WIRED | L149-156; uses VERCEL_REVALIDATE_URL env + VERCEL_REVALIDATE_SECRET |
| seed-status/route.ts | revalidatePath() | called when status=='complete' | WIRED | L41-50 looks up tenant slug then `revalidatePath('/' + slug)` |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| TenantDetailClient.tsx | `menuProducts` | GET /products-list?menuId=... | DB query: `service.from('products').select(...).eq('tenant_id', tenantId).eq('menu_id', menuId)` | FLOWING (real DB query) |
| TenantDetailClient.tsx | `imageSeedJobId`, `imageSeedStatus` | POST /seed-images then poll /seed-status | Real DB inserts/reads in ai_jobs table | FLOWING (subject to human verification of full round-trip) |
| seed-images/route.ts | `jobId` returned to client | `service.from('ai_jobs').insert(...).select('id').single()` | Real DB insert | FLOWING |
| seed-status/route.ts | `{ status, errorMessage }` | `service.from('ai_jobs').select('status, error_message').eq('id', jobId).eq('tenant_id', tenantId).single()` | Real DB read | FLOWING |
| scripts/seed-images.ts | cover/product URLs | Gemini API call -> Buffer -> Sharp -> Supabase Storage upload -> getPublicUrl | Real generation + upload (subject to env vars and API quota) | FLOWING (programmatic verification only — runtime test pending) |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| @google/genai resolvable from package.json | grep dependency | Found `^1.52.0` | PASS |
| No forbidden token `imageSize` in script | grep `imageSize` scripts/seed-images.ts | No matches | PASS |
| No forbidden token `Promise.all` in script | grep `Promise\.all` scripts/seed-images.ts | No matches | PASS |
| No `@/` alias imports in script | grep `from '@/'` scripts/seed-images.ts | No matches | PASS |
| No `.json()` on workflow_dispatch response | grep `await dispatchRes\.json` seed-images/route.ts | No matches | PASS |
| seed-images.ts runtime sanity (env vars missing safe) | n/a — script throws explicit "Missing required env vars" at L171-179 | Confirmed by code inspection | PASS |
| `npm run build` passes | Per 10-03-SUMMARY: build exit 0 with all routes registered | Reported by executor | PASS (per summary) |
| End-to-end Gemini -> Storage -> ISR revalidation | Requires real env + dispatch | Cannot run without secrets | SKIP (route to human) |

### Requirements Coverage

| Requirement | Source Plan(s) | Description | Status | Evidence |
| ----------- | -------------- | ----------- | ------ | -------- |
| AI-07 | 10-01, 10-02, 10-03 | Superadmin can trigger image seeding; system generates cover/banner photo via gpt-image-1-mini, converts to WebP via Sharp, uploads to Supabase Storage as cover | SATISFIED (with model substitution) | scripts/seed-images.ts L213-235 generates cover, uploads to `tenant-assets/${TENANT_ID}/cover.webp`, upserts `tenant_settings.banner_url`. **Note:** Plan and ROADMAP use `gemini-3.1-flash-image-preview` (Google) instead of `gpt-image-1-mini` (OpenAI) — this is a documented design decision in 10-CONTEXT.md / 10-RESEARCH.md (Gemini chosen for ecosystem alignment with Phase 9). REQUIREMENTS.md still names `gpt-image-1-mini` but the ROADMAP success criteria (the contract) names `gemini-3.1-flash-image-preview`. Treated as satisfied — model swap is intentional. |
| AI-08 | 10-01, 10-02, 10-03 | For each product without an image, system fetches a stock photo from Pexels or Unsplash, converts to WebP, uploads as image_url | SATISFIED (with source substitution) | scripts/seed-images.ts L238-317 generates AI image (NOT stock photo from Pexels/Unsplash) for each product where image_url IS NULL, converts to WebP, uploads, updates product.image_url. **Note:** REQUIREMENTS.md text mentions Pexels/Unsplash; ROADMAP SC2 says "AI photo" (Gemini-generated). Implementation matches ROADMAP. Treated as satisfied — generation source swap is intentional design decision. |
| AI-09 | 10-01, 10-02, 10-03 | Superadmin sees a "Seed image" button on each product row; clicking it generates an image for that single product | SATISFIED | TenantDetailClient.tsx L704-727 renders per-product list with `Seed image` buttons; products with image_url show 'has image' label (D-09 additive); button calls `handleSeedImages(product.id)`; products-list endpoint provides the data |

**Note on AI-07 / AI-08 model and source substitutions:** The original REQUIREMENTS.md text (written before plan/research) named `gpt-image-1-mini` and Pexels/Unsplash. The ROADMAP success criteria for Phase 10 (the binding contract) and the plan all use `gemini-3.1-flash-image-preview` and AI-generated photos. This was an intentional design decision documented in 10-CONTEXT.md and 10-RESEARCH.md. Recommend updating REQUIREMENTS.md to reflect the implemented behavior and to flip the AI-07/AI-08 status to Complete.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| (none) | - | No TODO/FIXME/PLACEHOLDER/HACK markers in any phase 10 file | - | - |
| (none) | - | No `imageSize` token (Pitfall 4 avoided) | - | - |
| (none) | - | No `Promise.all` for image generation (Pitfall 5 avoided) | - | - |
| (none) | - | No `@/` aliases in scripts/seed-images.ts (Pitfall — plain Node.js) | - | - |
| (none) | - | No `.json()` on workflow_dispatch response (Pitfall 1 avoided) | - | - |

Note from 10-02-SUMMARY threat flag (informational, not a blocker):
- `scripts/seed-images.ts` makes outbound fetch carrying `VERCEL_REVALIDATE_SECRET` to `${VERCEL_REVALIDATE_URL}/api/revalidate`. Since the URL host comes from a GH Actions secret, ensure repo-secret hygiene — a tampered secret value could leak the revalidate secret to an attacker-controlled origin. Flagged in summary; recommend documenting in repo SECURITY.md.

### Human Verification Required

10 items requiring human testing — see `human_verification` block in frontmatter:

1. **Apply migration 023 in Supabase SQL editor** — DDL is on disk but production DB application is manual (no local Supabase Docker)
2. **Set GitHub repository secrets** (5 secrets) — cannot verify presence from codebase
3. **Set Vercel env vars** (4 vars including GH_PAT) — cannot verify presence from codebase
4. **Trigger bulk image seeding (AI-07/AI-08) end-to-end** — confirm cover + product images generated, uploaded, banner_url + image_url populated
5. **Trigger per-product image seeding (AI-09) end-to-end** — confirm single product flow works
6. **Confirm skip-existing additive behavior (D-09)** — products with existing image_url show 'has image' label; bulk re-runs skip cover when banner_url already set
7. **Confirm public menu ISR cache invalidation** — visit /{tenantSlug} after seed completes
8. **Confirm failure path** — induced failure transitions ai_jobs.status to 'failed' and surfaces error in UI
9. **Confirm polling timeout** — UI shows timeout banner after ~5 min
10. **Confirm ai_usage telemetry** — row inserted with feature_key='image_seeding'

### Gaps Summary

**No code-level gaps detected.** Every must-have from the merged ROADMAP success criteria + PLAN frontmatter (13 truths) is verified at the artifact, wiring, and data-flow level. All three plans landed; all artifacts exist with substantive implementations; all key links are wired; no anti-patterns or hidden stubs found; all named pitfalls (Pitfall 1 — `.json()` on 204; Pitfall 4 — `imageSize`; Pitfall 5 — `Promise.all`) are demonstrably avoided via grep checks.

**Human verification required** because Phase 10 produces runtime behavior (Gemini API calls, GH Actions dispatch, Supabase Storage upload, ISR cache invalidation, polling round-trips) that cannot be exercised programmatically without real secrets and a triggered workflow. Additionally, the migration 023 must be applied manually in Supabase SQL editor (per 10-01-SUMMARY) before any of the above can succeed.

A small documentation drift exists between REQUIREMENTS.md (which still names `gpt-image-1-mini` and Pexels/Unsplash) and the ROADMAP success criteria + implemented code (which use Gemini and AI-generated photos). This is a documented design decision (10-CONTEXT.md / 10-RESEARCH.md) — recommend updating REQUIREMENTS.md to reflect the implemented model/source. Not a code gap.

---

_Verified: 2026-05-07_
_Verifier: Claude (gsd-verifier)_
