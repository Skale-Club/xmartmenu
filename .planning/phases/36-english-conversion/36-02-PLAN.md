---
phase: 36-english-conversion
plan: 02
type: execute
wave: 2
depends_on:
  - 36-01
files_modified: []
autonomous: true
requirements:
  - ENGL-01
  - ENGL-02
  - ENGL-03
  - ENGL-04
  - ENGL-05
  - ENGL-06

must_haves:
  truths:
    - "Grep for Portuguese patterns across all operator-facing paths returns zero matches"
    - "KDS status labels (Pending/Preparing/Ready/Done/Cancelled) are unchanged"
    - "KDS filter chips (Active/Pending/Preparing/Ready/All) are unchanged"
    - "KDS action buttons (Start preparing/Mark ready/Complete) are unchanged"
    - "Onboarding wizard steps are already English and remain unchanged"
    - "AdminSidebar navigation items are already English and remain unchanged"
    - "All 17 operator-facing files have zero Portuguese UI strings"
  artifacts:
    - path: "src/app/(admin)/settings/store/StoreClient.tsx"
      provides: "Post-conversion verified clean"
      contains: "Custom Domain"
    - path: "src/app/(superadmin)/tenants/TenantsClient.tsx"
      provides: "Post-conversion verified clean"
      contains: "Failed to update"
  key_links:
    - from: "grep scan output"
      to: "zero Portuguese matches"
      via: "pattern [À-ÿ]|Erro ao|Verificand|Salvar|Domínio"
      pattern: "zero lines returned"
---

<objective>
Run a comprehensive grep verification scan across all 17 operator-facing files to confirm zero Portuguese strings remain after Plan 01's replacements. If any residual strings are found in files not covered by Plan 01 (the "not scanned" files from UI-SPEC), apply targeted fixes.

Purpose: ENGL-01 through ENGL-06 — certify the full phase is complete across every operator-facing surface.
Output: Confirmed clean state or targeted fixes + final clean state. Phase 36 complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-english-conversion/36-CONTEXT.md
@.planning/phases/36-english-conversion/36-UI-SPEC.md
@.planning/phases/36-english-conversion/36-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full grep scan — verify zero Portuguese strings across all operator-facing paths</name>
  <files></files>
  <read_first>
    .planning/phases/36-english-conversion/36-UI-SPEC.md (Files in Scope section — the 17 files list)
    .planning/phases/36-english-conversion/36-01-SUMMARY.md
  </read_first>
  <action>
Run the following grep commands to detect any remaining Portuguese strings in all operator-facing paths. These commands are the verification gate for the entire phase.

**Scan 1 — accented characters (catches any Unicode 192–255 range characters used in Portuguese):**
```bash
grep -rn "[À-ÿ]" \
  src/app/\(admin\)/ \
  src/app/\(superadmin\)/ \
  src/app/api/admin/ \
  src/app/api/superadmin/ \
  src/app/onboarding/ \
  src/components/admin/
```

**Scan 2 — common Portuguese keyword patterns:**
```bash
grep -rn "Erro ao\|erro ao\|Não \|Verificand\|Salvar\|Domínio\|Configurar DNS\|registrador\|propagação\|verifique\|Evita loop\|faz logout\|Superadmin pode\|painel de qualquer" \
  src/app/\(admin\)/ \
  src/app/\(superadmin\)/ \
  src/app/api/admin/ \
  src/app/api/superadmin/ \
  src/app/onboarding/ \
  src/components/admin/
```

**Scan 3 — verify required English strings exist (Plan 01 output check):**
```bash
grep -n "Custom Domain" src/app/\(admin\)/settings/store/StoreClient.tsx
grep -n "Failed to update" src/app/\(superadmin\)/tenants/TenantsClient.tsx
grep -n "Failed to create restaurant" src/app/\(superadmin\)/tenants/TenantsClient.tsx
grep -n "Failed to delete" src/app/\(superadmin\)/tenants/TenantsClient.tsx
grep -n "Failed to save settings" src/app/\(superadmin\)/settings/SettingsClient.tsx
grep -n "Avoid infinite loop" src/app/\(admin\)/layout.tsx
grep -n "Superadmin can access any tenant" src/app/\(admin\)/layout.tsx
```

**Expected results:**
- Scan 1: Zero matches for accented characters in UI strings. (Currency symbols like R$, £, €, and emoji are NOT Portuguese strings — ignore false positives from CURRENCY_SYMBOL maps and BRL labels which are correct English abbreviations.)
- Scan 2: Zero matches for Portuguese keyword patterns.
- Scan 3: All 7 grep commands return at least one matching line each.

**If Scan 1 or 2 returns matches:**
Read the matching file(s) to identify the string. Apply targeted replacement following the UI-SPEC Tone and Voice Contract:
- Section headings: Title Case
- Body copy and toast messages: Sentence case
- CTA buttons: Active voice, Title Case
- Form labels (uppercase micro-labels): ALL CAPS only where class already includes `uppercase tracking-widest`
- Reference UI-SPEC Copywriting Contract for surface-specific phrasing

**Known acceptable false-positives in Scan 1** (do NOT change these):
- `BRL: 'R$'` — currency code abbreviation, not UI text
- `EUR: '€'`, `GBP: '£'` — currency symbols, not UI text
- `R$` in ingredient/product price display labels — currency symbol
- `🥗`, `📋`, `🎨`, `📱` etc — emoji in landing page content managed via superadmin settings UI (tenant-entered content, not hardcoded UI strings)
- `← Products`, `← Clients` — back navigation arrows using ASCII, not accented chars
  </action>
  <verify>
    <automated>grep -rn "[À-ÿ]" src/app/\(admin\)/ src/app/\(superadmin\)/ src/app/api/admin/ src/app/onboarding/ src/components/admin/ 2>/dev/null | grep -v "R\$\|BRL\|emoji\|🥗\|📋\|🎨\|📱\|📲\|📊\|🔗\|🔍\|⚡\|🏪\|currency\|CURRENCY_SYMBOL\|BRL: " | grep "[À-ÿ]"</automated>
  </verify>
  <acceptance_criteria>
    - Scan 1 returns zero matches for accented Portuguese characters in UI string literals (after excluding currency abbreviations)
    - Scan 2 returns zero matches for Portuguese keyword patterns
    - Scan 3: all 7 presence checks confirm English replacements are in place
    - If unexpected Portuguese strings were found and fixed, they are documented in the task output
  </acceptance_criteria>
  <done>Comprehensive grep scan passes — zero Portuguese UI strings remain across all 17 operator-facing files. Phase 36 English Conversion is complete.</done>
</task>

<task type="auto">
  <name>Task 2: Verify KDS and onboarding strings are preserved and unchanged</name>
  <files></files>
  <read_first>
    .planning/phases/36-english-conversion/36-UI-SPEC.md (KDS Filter Chips, KDS Status Labels, KDS Action Buttons, Onboarding Wizard sections)
  </read_first>
  <action>
Verify that the "already English" files have not been accidentally modified and retain correct English strings.

Run the following presence checks:

**KDS filter chip labels:**
```bash
grep -n "Start preparing\|Mark ready\|Complete\|Pending\|Preparing\|Ready\|Done\|Cancelled" src/app/\(admin\)/orders/OrdersClient.tsx | head -20
```

**KDS advance-action button labels (ADVANCE_LABEL record):**
```bash
grep -n "ADVANCE_LABEL\|Start preparing\|Mark ready" src/app/\(admin\)/orders/OrdersClient.tsx
```

**Onboarding wizard steps:**
```bash
grep -n "Welcome\|Contact info\|Your digital menu\|Your first product\|Menu created\|Continue\|Finish" src/app/onboarding/page.tsx | head -15
```

**AdminSidebar navigation:**
```bash
grep -n "Dashboard\|Categories\|Products\|Orders\|Ingredients\|Subscription\|Branding\|Sign out" src/components/admin/AdminSidebar.tsx | head -20
```

All checks must return matching lines confirming the English strings are present. If any of these files were accidentally modified in Plan 01, restore the original English values:
- KDS status union: `pending` | `preparing` | `ready` | `done` | `cancelled` (DB values — never changed)
- KDS display labels: `Pending` / `Preparing` / `Ready` / `Done` / `Cancelled`
- Filter chips: `Active` / `Pending` / `Preparing` / `Ready` / `All`
- Action buttons: `Start preparing` / `Mark ready` / `Complete`
  </action>
  <verify>
    <automated>grep -c "Start preparing\|Mark ready" src/app/\(admin\)/orders/OrdersClient.tsx</automated>
  </verify>
  <acceptance_criteria>
    - OrdersClient.tsx contains "Start preparing" and "Mark ready" (grep -c returns >= 1 for each)
    - onboarding/page.tsx contains "Welcome", "Continue", and "Finish"
    - AdminSidebar.tsx contains "Dashboard" and "Sign out"
    - No accidental regressions in any "already English" file
  </acceptance_criteria>
  <done>All pre-existing English strings in KDS, onboarding, and sidebar are confirmed present and unmodified. ENGL-03 and ENGL-04 are satisfied by the pre-existing English state of these files.</done>
</task>

</tasks>

<verification>
Final phase-level verification:

```bash
# Full scan — should return zero Portuguese UI strings
grep -rn "Erro ao\|Não \|Verificand\|Salvar\|Domínio\|Configurar DNS\|propagação" \
  src/app/\(admin\)/ src/app/\(superadmin\)/ src/app/onboarding/ src/components/admin/

# Confirm Plan 01 replacements are live
grep -n "Custom Domain" src/app/\(admin\)/settings/store/StoreClient.tsx
grep -n "Failed to" src/app/\(superadmin\)/tenants/TenantsClient.tsx
grep -n "Avoid infinite loop" src/app/\(admin\)/layout.tsx
```

All three commands must produce: first returns zero lines; second and third return at least one match each.
</verification>

<success_criteria>
- Zero Portuguese UI strings remain across all operator-facing paths (grep scan clean)
- All 7 Plan 01 English replacements confirmed present in target files
- KDS labels, filter chips, and action buttons unchanged and correct
- Onboarding wizard and AdminSidebar strings confirmed English and unmodified
- ENGL-01 through ENGL-06 all satisfied:
  - ENGL-01: Admin panel (StoreClient Custom Domain section converted; sidebar/nav already English)
  - ENGL-02: Superadmin panel (TenantsClient + SettingsClient error strings converted)
  - ENGL-03: Onboarding (already English — confirmed by scan)
  - ENGL-04: KDS (already English — confirmed by scan)
  - ENGL-05: Settings page (StoreClient is the settings file with most Portuguese; now clean)
  - ENGL-06: Error/validation messages (5 error strings replaced in TenantsClient + SettingsClient; StoreClient domain error messages replaced)
</success_criteria>

<output>
After completion, create `.planning/phases/36-english-conversion/36-02-SUMMARY.md`
</output>
