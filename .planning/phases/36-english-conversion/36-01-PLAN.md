---
phase: 36-english-conversion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(admin)/settings/store/StoreClient.tsx
  - src/app/(superadmin)/tenants/TenantsClient.tsx
  - src/app/(superadmin)/settings/SettingsClient.tsx
  - src/app/(admin)/layout.tsx
autonomous: true
requirements:
  - ENGL-01
  - ENGL-02
  - ENGL-05
  - ENGL-06

must_haves:
  truths:
    - "Admin Store Settings page shows 'Custom Domain' not 'Domínio Personalizado'"
    - "Admin Store Settings shows 'Your domain' label and 'Save Domain' button"
    - "Admin Store Settings shows 'Verify DNS' / 'Verifying...' buttons"
    - "Admin Store Settings shows 'Active' / 'Not verified' badges"
    - "Admin Store Settings DNS instructions use 'Configure DNS', 'Target:', and English body text"
    - "Superadmin Tenants panel shows English error messages for edit, create, and delete failures"
    - "Superadmin Platform Settings panel shows 'Failed to save settings' on save failure"
    - "Admin layout.tsx has no Portuguese code comments"
  artifacts:
    - path: "src/app/(admin)/settings/store/StoreClient.tsx"
      provides: "Custom Domain section in English"
      contains: "Custom Domain"
    - path: "src/app/(superadmin)/tenants/TenantsClient.tsx"
      provides: "English error messages"
      contains: "Failed to update"
    - path: "src/app/(superadmin)/settings/SettingsClient.tsx"
      provides: "English error fallback"
      contains: "Failed to save settings"
    - path: "src/app/(admin)/layout.tsx"
      provides: "English code comments"
      contains: "Avoid infinite loop"
  key_links:
    - from: "StoreClient.tsx handleSaveDomain/handleVerifyDomain"
      to: "verifyResult display block"
      via: "verifyResult.verified ternary"
      pattern: "Domain verified.*Your site is live"
    - from: "TenantsClient.tsx handleSaveEdit"
      to: "setError call"
      via: "res.ok check"
      pattern: "Failed to update"
---

<objective>
Convert all Portuguese UI strings in the admin panel Store Settings page (Custom Domain section) and the superadmin panel Tenants/Settings clients to English.

Purpose: ENGL-01, ENGL-05, ENGL-06 — operator-facing admin panel; ENGL-02, ENGL-06 — superadmin panel. This is a pure text replacement — no layout changes, no new components.
Output: 4 files with zero remaining Portuguese UI strings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/36-english-conversion/36-CONTEXT.md
@.planning/phases/36-english-conversion/36-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Portuguese strings in StoreClient.tsx (Custom Domain section)</name>
  <files>src/app/(admin)/settings/store/StoreClient.tsx</files>
  <read_first>
    src/app/(admin)/settings/store/StoreClient.tsx
    .planning/phases/36-english-conversion/36-UI-SPEC.md (String Mapping Contract — Store Settings section)
  </read_first>
  <action>
Apply the following exact string replacements in src/app/(admin)/settings/store/StoreClient.tsx.
No layout changes, no logic changes — text strings only.

REPLACE (line ~367):
  `Domínio Personalizado`
WITH:
  `Custom Domain`

REPLACE (line ~371):
  `Seu domínio`
WITH:
  `Your domain`

REPLACE (line ~386, the save button text):
  `{savingDomain ? '...' : 'Salvar'}`
WITH:
  `{savingDomain ? '...' : 'Save Domain'}`

REPLACE (line ~389, helper text):
  `Sem https:// — ex: sitedocliente.com`
WITH:
  `No https:// — e.g. yourdomain.com`

REPLACE (line ~399, verify button):
  `{verifying ? 'Verificando...' : 'Verificar DNS'}`
WITH:
  `{verifying ? 'Verifying...' : 'Verify DNS'}`

REPLACE (line ~403, verified badge):
  `<CheckCircle2 className="w-4 h-4" /> Ativo`
WITH:
  `<CheckCircle2 className="w-4 h-4" /> Active`

REPLACE (line ~407, not-verified badge):
  `<XCircle className="w-4 h-4" /> Não verificado`
WITH:
  `<XCircle className="w-4 h-4" /> Not verified`

REPLACE (line ~420, success message):
  `'Domínio verificado! Seu site está acessível em ' + customDomain`
WITH:
  `'Domain verified! Your site is live at ' + customDomain`

REPLACE (line ~421, error message):
  `` `Verificação falhou: ${verifyResult.reason ?? 'verifique o DNS'}` ``
WITH:
  `` `Verification failed: ${verifyResult.reason ?? 'check your DNS settings'}` ``

REPLACE (line ~427, DNS instructions heading):
  `Configurar DNS`
WITH:
  `Configure DNS`

REPLACE (line ~428, DNS instructions body):
  `No painel do seu registrador de domínio, crie um registro CNAME:`
WITH:
  `In your domain registrar's panel, create a CNAME record:`

REPLACE (line ~431, Tipo field):
  `<span className="font-semibold text-blue-800">Tipo:</span>`
WITH:
  `<span className="font-semibold text-blue-800">Type:</span>`

REPLACE (line ~432, Host field — Host is already correct English, leave unchanged)
  No change needed for `Host:` — already English.

REPLACE (line ~433, Destino field):
  `<span className="font-semibold text-blue-800">Destino:</span>`
WITH:
  `<span className="font-semibold text-blue-800">Target:</span>`

REPLACE (line ~434, propagation note):
  `A propagação pode levar até 24 horas.`
WITH:
  `DNS propagation may take up to 24 hours.`

Also update the domain input placeholder (line ~377):
  `placeholder="sitedocliente.com"`
WITH:
  `placeholder="yourdomain.com"`
  </action>
  <verify>
    <automated>grep -n "Domínio\|Seu domínio\|Salvar\|Verificando\|Verificar DNS\|Ativo\|Não verificado\|Domínio verificado\|Verificação falhou\|verifique o DNS\|Configurar DNS\|registrador\|propagação\|Destino:" src/app/\(admin\)/settings/store/StoreClient.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep returns zero matches for: Domínio, Seu domínio, Salvar, Verificando, Verificar DNS, Ativo, Não verificado, Domínio verificado, Verificação falhou, verifique o DNS, Configurar DNS, registrador, propagação, Destino:
    - File contains: "Custom Domain", "Your domain", "Save Domain", "No https:// — e.g. yourdomain.com", "Verifying...", "Verify DNS", "Active", "Not verified", "Domain verified! Your site is live at", "Verification failed:", "check your DNS settings", "Configure DNS", "In your domain registrar's panel", "Type:", "Target:", "DNS propagation may take up to 24 hours."
  </acceptance_criteria>
  <done>StoreClient.tsx Custom Domain section contains zero Portuguese UI strings; all 15 replacements applied per UI-SPEC string mapping table.</done>
</task>

<task type="auto">
  <name>Task 2: Replace Portuguese error strings in TenantsClient.tsx, SettingsClient.tsx, and layout.tsx comments</name>
  <files>
    src/app/(superadmin)/tenants/TenantsClient.tsx
    src/app/(superadmin)/settings/SettingsClient.tsx
    src/app/(admin)/layout.tsx
  </files>
  <read_first>
    src/app/(superadmin)/tenants/TenantsClient.tsx (lines 112–190)
    src/app/(superadmin)/settings/SettingsClient.tsx (line 69)
    src/app/(admin)/layout.tsx (lines 28–36)
    .planning/phases/36-english-conversion/36-UI-SPEC.md (Superadmin Tenants Panel, Superadmin Settings Panel, Admin Layout Comments sections)
  </read_first>
  <action>
**TenantsClient.tsx — 4 string replacements:**

REPLACE (line 127, in handleSaveEdit):
  `setError('Erro ao editar: ' + data.error)`
WITH:
  `setError('Failed to update: ' + data.error)`

REPLACE (line 146, in handleCreate):
  `setError(data.error ?? 'Erro ao criar cliente')`
WITH:
  `setError(data.error ?? 'Failed to create restaurant')`

REPLACE (line 177, in handleDelete — first occurrence):
  `setError('Erro ao excluir: ' + data.error)`
WITH:
  `setError('Failed to delete: ' + data.error)`

REPLACE (line 185, in handleDelete — second occurrence):
  `setError('Erro ao excluir: ' + data.error)`
WITH:
  `setError('Failed to delete: ' + data.error)`

---

**SettingsClient.tsx — 1 string replacement:**

REPLACE (line 69):
  `setError(d.error ?? 'Erro ao salvar')`
WITH:
  `setError(d.error ?? 'Failed to save settings')`

---

**layout.tsx — 2 comment replacements:**

REPLACE (line 30):
  `// Evita loop infinito: faz logout antes de redirecionar`
WITH:
  `// Avoid infinite loop: sign out before redirecting`

REPLACE (line 35):
  `// Superadmin pode acessar o painel de qualquer tenant via cookie`
WITH:
  `// Superadmin can access any tenant's panel via preview cookie`
  </action>
  <verify>
    <automated>grep -n "Erro ao\|ao editar\|ao criar\|ao excluir\|ao salvar\|Evita loop\|faz logout\|Superadmin pode\|painel de qualquer" src/app/\(superadmin\)/tenants/TenantsClient.tsx src/app/\(superadmin\)/settings/SettingsClient.tsx src/app/\(admin\)/layout.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep returns zero matches across all three files for: "Erro ao", "ao editar", "ao criar", "ao excluir", "ao salvar", "Evita loop", "faz logout", "Superadmin pode", "painel de qualquer"
    - TenantsClient.tsx contains: "Failed to update:", "Failed to create restaurant", "Failed to delete:"
    - SettingsClient.tsx line 69 contains: "Failed to save settings"
    - layout.tsx contains: "Avoid infinite loop: sign out before redirecting" and "Superadmin can access any tenant's panel via preview cookie"
  </acceptance_criteria>
  <done>7 Portuguese strings replaced across 3 files — superadmin error messages and admin layout comments are fully in English per UI-SPEC mapping tables.</done>
</task>

</tasks>

<verification>
Run final grep scan to confirm zero Portuguese strings remain across all 4 files:

```bash
grep -n "[À-ÿ]\|Erro ao\|erro ao\|Não \|Verificand\|Salvar\|Domínio\|Configurar DNS\|registrador\|propagação\|verifique\|Evita\|faz logout\|Superadmin pode" \
  src/app/\(admin\)/settings/store/StoreClient.tsx \
  src/app/\(superadmin\)/tenants/TenantsClient.tsx \
  src/app/\(superadmin\)/settings/SettingsClient.tsx \
  src/app/\(admin\)/layout.tsx
```

Expected result: zero output.
</verification>

<success_criteria>
- StoreClient.tsx: all 15 Portuguese strings in Custom Domain section replaced with English equivalents per UI-SPEC table
- TenantsClient.tsx: all 4 Portuguese error strings replaced (lines 127, 146, 177, 185)
- SettingsClient.tsx: "Erro ao salvar" replaced with "Failed to save settings" (line 69)
- layout.tsx: both Portuguese code comments replaced with English equivalents (lines 30, 35)
- Zero Portuguese UI strings remain in any of the 4 files
- Zero layout, logic, or styling changes — text only
</success_criteria>

<output>
After completion, create `.planning/phases/36-english-conversion/36-01-SUMMARY.md`
</output>
