---
phase: 5
slug: admin-product-options-ui
status: draft
shadcn_initialized: false
preset: none
created: 2026-05-06
---

# Phase 5 — UI Design Contract

> Visual and interaction contract for Phase 5: Admin Product Options UI.
> Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — manual Tailwind v4 |
| Preset | not applicable |
| Component library | none (custom components only) |
| Icon library | lucide-react |
| Font | Inter (Google Fonts via next/font/google) |

Source: detected from `src/app/layout.tsx`, `package.json`, `src/components/admin/CopyMenuUrl.tsx`

No `components.json` found. The project uses a manual Tailwind v4 design system with established inline class conventions across all admin pages. Do NOT initialize shadcn — follow existing conventions.

---

## Spacing Scale

Declared values (multiples of 4 only):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, badge padding (`px-1`, `py-0.5`, `gap-1`) |
| sm | 8px | Compact form field gaps, inline button spacing (`gap-2`, `px-2`, `py-1`) |
| md | 16px | Default form field spacing, card inner padding (`p-4`, `gap-4`) |
| lg | 24px | Section padding, header bottom margin (`mb-6`, `p-6`) |
| xl | 32px | Page outer padding (`p-8`) |
| 2xl | 48px | Empty state vertical padding (`py-12`) |
| 3xl | 64px | Full empty state centering (`py-16`) |

Exceptions:
- Touch/click targets: minimum 32px height on all interactive row controls (Edit, Delete, up/down arrows)
- Inline expand-form rows: `py-3 px-4` (12px + 16px) to match card inner rhythm
- Group header row height: 48px min to give adequate tap target for reorder arrows

Source: measured from existing `ProductsClient.tsx` and `ConfirmDialog.tsx` class patterns.

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px (text-sm) | 400 (regular) | 1.5 |
| Label | 14px (text-sm) | 500 (medium) | 1.4 |
| Heading (page) | 24px (text-2xl) | 700 (bold) | 1.2 |
| Subheading (section title) | 16px (text-base) | 600 (semibold) | 1.3 |

Source: detected from `ProductsClient.tsx` — `text-2xl font-bold` for page heading, `text-sm font-semibold` for card titles, `text-sm font-medium` for labels, `text-sm` body.

Note: Inter is a variable font; Tailwind maps `font-medium` = 500, `font-semibold` = 600, `font-bold` = 700. Declared weights are 400 (regular) and 600 (semibold) as the two primary weights — 700 reserved for the single page-level heading, and 500 for form labels only.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | zinc-50 (`#fafafa`) | Page background (`bg-zinc-50`), layout surface |
| Secondary (30%) | white (`#ffffff`) | Cards, modal backgrounds, form panels (`bg-white`) |
| Accent (10%) | zinc-900 (`#18181b`) | Primary buttons, active filter pills, focus rings |
| Destructive | red-600 (`#dc2626`) | Delete group button, delete option button, ConfirmDialog confirm button |

Accent reserved for:
- Primary CTA buttons only ("Save group", "Save option", "+ Add group", "+ Add option")
- Active category filter pill background
- Input focus ring (`focus:ring-2 focus:ring-zinc-900`)

Secondary semantic colors (informational, not accent):
- green-100 / green-700: "Available" option badge
- zinc-100 / zinc-500: "Unavailable" option badge
- amber-50 / amber-700: warning banners (no menu selected state)
- red-50 / red-600: inline error messages

Source: detected from `ProductsClient.tsx`, `ConfirmDialog.tsx`, admin layout (`bg-zinc-50`).

---

## Component Inventory

Reuse these existing components exactly — do not create new variants:

| Component | File | Usage in this phase |
|-----------|------|---------------------|
| ConfirmDialog | `src/components/ui/ConfirmDialog.tsx` | Delete group confirmation, delete option confirmation |
| Modal (inline) | defined inside `ProductsClient.tsx` | Reference style only — detail page does NOT use a modal |

New components to create for this phase:

| Component | Location | Description |
|-----------|----------|-------------|
| ProductDetailClient | `src/app/(admin)/menu/products/[id]/ProductDetailClient.tsx` | Main client component hosting product fields + option groups section |
| OptionGroupRow | inline or separate file | Collapsed group row with name, type badge, option count, edit/delete/reorder controls |
| OptionGroupForm | inline or separate file | Inline expand-form for creating or editing a group |
| OptionRow | inline or separate file | Collapsed option row with name, price, availability toggle, edit/delete/reorder controls |
| OptionForm | inline or separate file | Inline expand-form for creating or editing an option |

---

## Interaction Contract

### Page Layout: `/admin/menu/products/[id]`

```
┌─ Page header ──────────────────────────────────────────┐
│  ← Back to Products     [Product Name]  (text-2xl bold) │
├─ Product fields section ───────────────────────────────┤
│  (name, description, price, original_price, category,  │
│   images, tags, is_featured) — same fields as modal     │
│  [Save product] button at bottom of section             │
├─ Option Groups section ────────────────────────────────┤
│  "Option Groups" heading (text-base semibold)           │
│  [+ Add group] button (right-aligned)                   │
│                                                         │
│  ┌─ Group row (collapsed) ──────────────────────────┐   │
│  │  [↑][↓]  Group Name  [badge: type]  N options    │   │
│  │                             [Edit] [Delete]      │   │
│  └──────────────────────────────────────────────────┘   │
│                                                         │
│  ┌─ Group row (expanded for edit/add) ──────────────┐   │
│  │  Name: [_______________]                         │   │
│  │  Type: [single ▼]  Required: [checkbox]          │   │
│  │  Min selections: [_]  Max selections: [_]        │   │
│  │  [Save group]  [Cancel]                          │   │
│  └──────────────────────────────────────────────────┘   │
│                                                         │
│  ┌─ Group header row (with options list) ────────────┐  │
│  │  [↑][↓]  Tamanho  [single]  3 options  [Edit][Del]│  │
│  │  ┌─ Option row ───────────────────────────────┐   │  │
│  │  │  [↑][↓]  Pequena  R$ 29,90  [Available ●]  │   │  │
│  │  │                          [Edit] [Delete]   │   │  │
│  │  └────────────────────────────────────────────┘   │  │
│  │  [+ Add option]                                   │  │
│  └────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────┘
```

### Inline expand-form behavior
- "+ Add group" click: appends a new blank OptionGroupForm below existing group rows. Form is visible; other "+ Add group" button is disabled while form is open.
- "Edit" on group row: that row transforms in place into OptionGroupForm pre-filled with current values. No modal.
- "Cancel" on form: form closes with no changes. Row returns to collapsed state.
- "Save" on form: optimistic UI update + Supabase upsert. On error: inline error shown inside the form. On success: form collapses.
- Same pattern applies to OptionForm within each group.

### Position reorder
- "↑" arrow: decrements position by 1 (swaps with the row above). Disabled when row is already first.
- "↓" arrow: increments position by 1 (swaps with the row below). Disabled when row is already last.
- Arrow buttons are styled as `text-zinc-400 hover:text-zinc-700 p-1 rounded` (icon-only, 24px Lucide arrows).
- Position persists via Supabase update immediately on click.

### Availability toggle on options
- Toggle renders as a pill badge: `bg-green-100 text-green-700` when available, `bg-zinc-100 text-zinc-500` when unavailable.
- Clicking the pill toggles `is_available` immediately via Supabase update (no confirmation required).

### Adaptive price field (D-07)
- Group type `single` or `half_and_half`: show label "Base price (full size price)" — input writes `base_price`, sets `price_modifier = 0`.
- Group type `multiple`: show label "Price modifier (+/-)" — input writes `price_modifier`, keeps `base_price = null`.
- Field label and hint update reactively when group type select changes.
- Both inputs: `type="number" step="0.01"` with currency prefix span matching existing pattern.

### Type badge
- Renders as a small rounded pill next to the group name.
- `single`: `bg-blue-100 text-blue-700 text-xs px-2 py-0.5 rounded-full`
- `multiple`: `bg-purple-100 text-purple-700 text-xs px-2 py-0.5 rounded-full`
- `half_and_half`: `bg-orange-100 text-orange-700 text-xs px-2 py-0.5 rounded-full`

### Loading / saving states
- "Save group" / "Save option" button: disabled + text changes to "Saving..." while mutation is in-flight. Matches existing `disabled:opacity-50` pattern.
- Reorder arrows: briefly disabled (pointer-events-none) while position update is in-flight to prevent double-click.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Page heading | "Edit product" |
| Back link | "← Products" |
| Option groups section heading | "Option Groups" |
| Primary CTA — save product fields | "Save product" |
| Primary CTA — save group | "Save group" |
| Primary CTA — save option | "Save option" |
| Add group trigger | "+ Add group" |
| Add option trigger | "+ Add option" |
| Group form saving state | "Saving..." |
| Option form saving state | "Saving..." |
| Cancel button | "Cancel" |
| Empty state — no option groups heading | "No option groups yet" |
| Empty state — no option groups body | "Add a group to offer sizes, toppings, or half-and-half options for this product." |
| Empty state — no options in a group heading | "No options yet" |
| Empty state — no options in a group body | "Add options to this group." |
| Delete group — ConfirmDialog title | "Delete option group" |
| Delete group — ConfirmDialog message | "Delete this option group and all its options? This action cannot be undone." |
| Delete group — ConfirmDialog confirm label | "Delete group" |
| Delete option — ConfirmDialog title | "Delete option" |
| Delete option — ConfirmDialog message | "Delete this option? This action cannot be undone." |
| Delete option — ConfirmDialog confirm label | "Delete option" |
| Inline form error | "Failed to save. [error message from Supabase]" |
| Reorder error (silent) | No UI — silently retry once, then restore previous order |
| Availability toggle — available | "Available" |
| Availability toggle — unavailable | "Unavailable" |
| Required group label | "Required" (badge: `bg-red-100 text-red-700 text-xs px-2 py-0.5 rounded-full`) |
| Optional group label | no badge |
| Price field label — single/half_and_half | "Base price (full size price)" |
| Price field label — multiple | "Price modifier (+/-)" |
| Price field hint — single/half_and_half | "Absolute price for this option size" |
| Price field hint — multiple | "Amount added to or subtracted from the product's base price" |
| Option count summary | "1 option" / "N options" |

Source: CONTEXT.md D-04, D-05, D-07, D-08, D-09. Copywriting defaults applied for all items not specified upstream.

---

## States Required

Each interactive element must implement these states:

| Element | States |
|---------|--------|
| OptionGroupRow | collapsed (default), expanded-edit, saving, error |
| OptionGroupForm (add) | empty, filling, saving, error |
| OptionRow | collapsed (default), expanded-edit, saving, error |
| OptionForm (add) | empty, filling, saving, error |
| Availability toggle | available, unavailable, transitioning |
| Reorder arrows (↑↓) | default, disabled (first/last position), in-flight |
| Product fields section | default, saving, error, success (brief) |
| Page (whole) | loading (server component), loaded |
| Option groups section | loading, empty, populated |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| none | n/a | not applicable — no shadcn, no third-party registries |

No component registries are in use. All components are hand-written Tailwind. Registry vetting gate: not required.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
