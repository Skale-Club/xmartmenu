---
phase: 50-schema-contract
plan: 03
type: execute
wave: 2
depends_on: ["50-02"]
files_modified:
  - scripts/xphere-mapping-check.ts
  - package.json
autonomous: true
requirements: [FND-02]
must_haves:
  truths:
    - "A developer can verify the mapper offline with no QStash and no Xphere credentials"
    - "The check asserts payload shape, external_id keying, stage selection, and MRR normalization against fixture rows"
    - "The check exits non-zero on any assertion failure so it is usable as a gate"
  artifacts:
    - path: "scripts/xphere-mapping-check.ts"
      provides: "tsx assertion script exercising buildSyncPayload/normalizeMrr/selectStage against fixtures"
      contains: "buildSyncPayload"
    - path: "package.json"
      provides: "npm run xphere:check script entry following the npx tsx convention"
      contains: "xphere:check"
  key_links:
    - from: "scripts/xphere-mapping-check.ts"
      to: "src/lib/xphere/mapping.ts"
      via: "import of buildSyncPayload + helpers"
      pattern: "@/lib/xphere/mapping|\\.\\./src/lib/xphere/mapping"
    - from: "package.json"
      to: "scripts/xphere-mapping-check.ts"
      via: "npm script invoking npx tsx"
      pattern: "xphere-mapping-check"
---

<objective>
Deliver Phase 50 success criterion 4: the pure mapper is exercised offline with no network. A `tsx` assertion script asserts the `buildSyncPayload` payload shape, `external_id` keying, stage selection, and MRR normalization against fixture rows — runnable with zero QStash/Xphere credentials. The repo has no test runner; per the CONTEXT decision we use a `tsx` script (matching the existing `scripts/*.ts` convention) rather than introducing vitest.

Purpose: Lock in the riskiest correctness decisions (idempotency key, stage model, normalized MRR) behind a runnable gate before any network code exists.
Output: `scripts/xphere-mapping-check.ts` + an `xphere:check` npm script.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-schema-contract/50-CONTEXT.md
@src/lib/xphere/mapping.ts
@src/lib/xphere/types.ts

<interfaces>
<!-- Produced by Plan 50-02. The check imports these. -->
<!-- mapping.ts exports: buildSyncPayload(input: SyncMapInput): XphereSyncRequest, normalizeMrr(plan), selectStage(reason, status). -->
<!-- SyncMapInput = { tenant:{id,slug,name,custom_domain}, owner:{full_name,role}|null, -->
<!--                  plan:{monthly_price,annual_price,billing_cycle,status}, currency, reason, eventId?, tags? } -->
<!-- types.ts exports: XPHERE_STAGES, SyncReason, XphereStage, XphereSyncRequest. -->

<!-- Path alias: @/* -> ./src/* (tsconfig). scripts/*.ts run via `npx tsx`, e.g. stripe-setup-plans.ts. -->
<!-- tsx@^4.19.0 is already installed. tsx resolves the @/ alias from tsconfig paths. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the offline tsx assertion script + npm run xphere:check</name>
  <read_first>
    - src/lib/xphere/mapping.ts (exact exports + SyncMapInput shape to build fixtures against)
    - src/lib/xphere/types.ts (XPHERE_STAGES values to assert stage selection against)
    - scripts/stripe-setup-plans.ts (the npx tsx script convention — but this check needs NO env/dotenv/creds)
    - package.json (scripts block — add the new entry next to stripe:setup-plans)
  </read_first>
  <files>scripts/xphere-mapping-check.ts, package.json</files>
  <action>
    Create `scripts/xphere-mapping-check.ts` — a self-contained assertion script using only Node's `assert` and the pure mapper. It MUST NOT import dotenv, Supabase, QStash, or any network module, and MUST NOT read env vars (it runs with no credentials).

    Import from the alias: `import { buildSyncPayload, normalizeMrr, selectStage } from '@/lib/xphere/mapping'` and `import { XPHERE_STAGES } from '@/lib/xphere/types'`.

    Use `import assert from 'node:assert/strict'`. Define fixture inputs and assert at minimum:

    1. MRR normalization:
       - `normalizeMrr({ billing_cycle: 'annual', annual_price: 1200, monthly_price: 999, status: 'active' })` === `100`.
       - `normalizeMrr({ billing_cycle: 'monthly', annual_price: 1200, monthly_price: 49, status: 'active' })` === `49`.

    2. Stage selection (drive from XPHERE_STAGES, not string literals):
       - `selectStage('onboarded', 'trial')` === `XPHERE_STAGES.ONBOARDING`.
       - `selectStage('plan_activated', 'active')` === `XPHERE_STAGES.ACTIVE`.
       - `selectStage('past_due', 'past_due')` === `XPHERE_STAGES.AT_RISK`.
       - `selectStage('churned', 'cancelled')` === `XPHERE_STAGES.CHURNED`.

    3. Payload shape + external_id keying, using a tenant fixture `{ id: 't-123', slug: 'acme', name: 'Acme', custom_domain: null }`, owner `{ full_name: 'Jane Owner', role: 'store-admin' }`, an annual plan (annual_price 1200), currency 'brl', reason 'plan_activated':
       - `payload.source` === `'xmartmenu'`.
       - `payload.account.external_id` === `'t-123'` AND `payload.contact.external_id` === `'t-123'` AND `payload.opportunity.external_id` === `'t-123'`.
       - `payload.contact.role` === `'store-admin'` AND `payload.contact.name` === `'Jane Owner'`.
       - `payload.opportunity.stage` === `XPHERE_STAGES.ACTIVE`.
       - `payload.opportunity.amount` === `100` AND `payload.opportunity.currency` === `'brl'`.

    4. Onboarding note dedup fallback:
       - For `reason: 'onboarded'` with no `eventId`, `payload.note?.dedup_id` === `'onboarding:t-123'`.
       - For an event-driven reason with `eventId: 'evt_42'`, `payload.note?.dedup_id` === `'evt_42'`.

    On all assertions passing, `console.log('xphere-mapping-check: all assertions passed')` and exit 0 (default). Node's `assert/strict` throws on failure → non-zero exit automatically; do not catch it.

    Then add to `package.json` `scripts`:
    ```json
    "xphere:check": "npx tsx scripts/xphere-mapping-check.ts"
    ```
    (insert after `"stripe:setup-plans"`; preserve all existing scripts and valid JSON).

    No new npm dependency — `tsx` is already installed.
  </action>
  <verify>
    <automated>npx tsx scripts/xphere-mapping-check.ts && grep -q "xphere:check" package.json && grep -q "@/lib/xphere/mapping" scripts/xphere-mapping-check.ts && node -e "require('./package.json')" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsx scripts/xphere-mapping-check.ts` exits 0 and prints `xphere-mapping-check: all assertions passed`.
    - The script imports `buildSyncPayload` from `@/lib/xphere/mapping` and `XPHERE_STAGES` from `@/lib/xphere/types`.
    - The script does NOT import dotenv, Supabase, `@upstash/qstash`, or any network/`fetch` module, and reads no env vars (grep -L confirms no `process.env`, no `dotenv`, no `createClient`).
    - `package.json` contains an `xphere:check` script and remains valid JSON (`node -e "require('./package.json')"` succeeds).
    - The script uses `node:assert/strict` so a failed assertion produces a non-zero exit.
  </acceptance_criteria>
  <done>An offline, credential-free `npm run xphere:check` asserts MRR normalization, stage selection, external_id keying, payload shape, and note dedup against fixtures — exiting 0 on pass, non-zero on any failure.</done>
</task>

</tasks>

<verification>
- `npx tsx scripts/xphere-mapping-check.ts` runs with no QStash/Xphere credentials and exits 0.
- Assertions cover payload shape, `external_id = tenant.id` keying on all three entities, stage selection via XPHERE_STAGES, MRR normalization (annual/12 and monthly), and note dedup fallback.
- `npm run xphere:check` is wired and `package.json` stays valid JSON.
</verification>

<success_criteria>
- Phase 50 success criterion 4 satisfied: the pure mapper is exercised offline, no network, no credentials.
- The check is a runnable gate (non-zero exit on failure), not a manual eyeball.
- No vitest introduced (tsx script per CONTEXT decision); no new npm dependency; Xphere repo untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/50-schema-contract/50-03-SUMMARY.md`
</output>
