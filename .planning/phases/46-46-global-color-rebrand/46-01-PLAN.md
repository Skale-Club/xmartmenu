---
plan_id: "46-01"
phase: 46
wave: 1
objective: >
  Update the platform primary color from #EEFF00 to #F52323 by changing CSS variables,
  replacing all 14 hardcoded hex fallbacks, updating admin default values, and fixing
  the marketing hero gradient. This plan handles everything except the Tailwind class
  surgical replacements (covered in 46-02).
requirements_addressed:
  - COLOR-01
  - COLOR-02
  - COLOR-03
  - COLOR-05
  - COLOR-06
files_modified:
  - src/app/globals.css
  - src/app/(marketing)/layout.tsx
  - src/app/(superadmin)/layout.tsx
  - src/app/(admin)/layout.tsx
  - src/app/(public)/[slug]/page.tsx
  - src/app/(public)/[slug]/[menuSlug]/page.tsx
  - src/app/(public)/[slug]/waiter/page.tsx
  - src/app/(public)/[slug]/me/page.tsx
  - src/app/(public)/[slug]/me/login/page.tsx
  - src/components/menu/MenuPage.tsx
  - src/components/menu/AiChatWidget.tsx
  - src/app/api/onboarding/route.ts
  - src/app/(superadmin)/settings/SettingsClient.tsx
  - src/app/(admin)/settings/branding/BrandingClient.tsx
  - src/app/(marketing)/ClientPage.tsx
depends_on: []
autonomous: true
---

# Plan 46-01: CSS Vars + Hex Fallbacks + Gradient + Admin Defaults

## ATOMIC COMMIT CONSTRAINT

This plan is Wave 1 of an atomic two-plan operation. Neither 46-01 nor 46-02 should be
committed independently. All file changes from BOTH 46-01 and 46-02 must land in a
single git commit (executed at the end of 46-02). Splitting creates broken intermediate
state: dark `text-zinc-950` text appears on red `bg-primary` buttons until 46-02's class
replacements are applied.

---

## Task 1 — Update CSS custom properties in globals.css

<task id="46-01-T1">

<read_first>
- src/app/globals.css — lines 1-20 (`:root` block containing `--primary` and `--primary-foreground`)
</read_first>

<action>
In `src/app/globals.css`, make two changes inside the `:root` block:

1. Line 6: Change `--primary: #EEFF00;` to `--primary: #F52323;`
2. Line 7: Change `--primary-foreground: #09090b;` to `--primary-foreground: #ffffff;`

No other lines in globals.css are touched.
</action>

<acceptance_criteria>
- `grep -n "primary" src/app/globals.css` must show `--primary: #F52323;` on line 6 and `--primary-foreground: #ffffff;` on line 7
- No occurrence of `#EEFF00` or `#09090b` remains in globals.css
- `grep -c "EEFF00" src/app/globals.css` returns 0
</acceptance_criteria>

</task>

---

## Task 2 — Replace 14 hardcoded #EEFF00 fallback hex values

<task id="46-01-T2">

<read_first>
- src/app/(marketing)/layout.tsx — line 68 (`ps?.cta_color ?? '#EEFF00'`)
- src/app/(superadmin)/layout.tsx — line 36 (`ps?.cta_color ?? '#EEFF00'`)
- src/app/(admin)/layout.tsx — lines 65, 111 (two `#EEFF00` fallbacks)
- src/app/(public)/[slug]/page.tsx — lines 90, 193 (two `#EEFF00` fallbacks)
- src/app/(public)/[slug]/[menuSlug]/page.tsx — line 202
- src/app/(public)/[slug]/waiter/page.tsx — line 105
- src/app/(public)/[slug]/me/page.tsx — line 52
- src/app/(public)/[slug]/me/login/page.tsx — line 22
- src/components/menu/MenuPage.tsx — lines 106, 790 (two `#EEFF00` fallbacks)
- src/components/menu/AiChatWidget.tsx — line 25 (default prop `primaryColor = '#EEFF00'`)
- src/app/api/onboarding/route.ts — line 65 (`{ primary: '#EEFF00', accent: '#09090b' }`)
</read_first>

<action>
In each file listed above, replace every occurrence of `'#EEFF00'` with `'#F52323'`.
This is a purely mechanical string substitution — only the hex value changes, no
surrounding code structure is altered.

Specific substitutions:

1. `src/app/(marketing)/layout.tsx:68` — `?? '#EEFF00'` → `?? '#F52323'`
2. `src/app/(superadmin)/layout.tsx:36` — `?? '#EEFF00'` → `?? '#F52323'`
3. `src/app/(admin)/layout.tsx:65` — `?? '#EEFF00'` → `?? '#F52323'`
4. `src/app/(admin)/layout.tsx:111` — `?? '#EEFF00'` → `?? '#F52323'`
5. `src/app/(public)/[slug]/page.tsx:90` — `?? '#EEFF00'` → `?? '#F52323'`
6. `src/app/(public)/[slug]/page.tsx:193` — `?? '#EEFF00'` → `?? '#F52323'`
7. `src/app/(public)/[slug]/[menuSlug]/page.tsx:202` — `?? '#EEFF00'` → `?? '#F52323'`
8. `src/app/(public)/[slug]/waiter/page.tsx:105` — `?? '#EEFF00'` → `?? '#F52323'`
9. `src/app/(public)/[slug]/me/page.tsx:52` — `'#EEFF00'` → `'#F52323'`
10. `src/app/(public)/[slug]/me/login/page.tsx:22` — `?? '#EEFF00'` → `?? '#F52323'`
11. `src/components/menu/MenuPage.tsx:106` — `?? '#EEFF00'` → `?? '#F52323'`
12. `src/components/menu/MenuPage.tsx:790` — `?? '#EEFF00'` → `?? '#F52323'`
13. `src/components/menu/AiChatWidget.tsx:25` — `primaryColor = '#EEFF00'` → `primaryColor = '#F52323'`
14. `src/app/api/onboarding/route.ts:65` — `{ primary: '#EEFF00', accent: '#09090b' }` → `{ primary: '#F52323', accent: '#09090b' }`
</action>

<acceptance_criteria>
- `grep -rn "EEFF00" src/ --include="*.tsx" --include="*.ts"` returns zero matches
- `grep -rn "F52323" src/ --include="*.tsx" --include="*.ts" | wc -l` returns at least 14
- TypeScript compilation passes: `npx tsc --noEmit` exits 0
</acceptance_criteria>

</task>

---

## Task 3 — Replace #CBFF00 default cta_color in SettingsClient.tsx

<task id="46-01-T3">

<read_first>
- src/app/(superadmin)/settings/SettingsClient.tsx — line 241 (`cta_color: s.cta_color ?? '#CBFF00'`)
</read_first>

<action>
In `src/app/(superadmin)/settings/SettingsClient.tsx`, line 241:
Change `s.cta_color ?? '#CBFF00'` to `s.cta_color ?? '#F52323'`

Note: this is `#CBFF00` (the superadmin settings default), distinct from the `#EEFF00`
fallbacks in Task 2.
</action>

<acceptance_criteria>
- `grep -n "CBFF00" src/app/(superadmin)/settings/SettingsClient.tsx` returns zero matches
- `grep -n "cta_color" src/app/(superadmin)/settings/SettingsClient.tsx` shows `#F52323` as the fallback
</acceptance_criteria>

</task>

---

## Task 4 — Update default branding palette in BrandingClient.tsx

<task id="46-01-T4">

<read_first>
- src/app/(admin)/settings/branding/BrandingClient.tsx — line 24 (`{ name: 'Default', primary: '#EEFF00', accent: '#09090b' }`)
</read_first>

<action>
In `src/app/(admin)/settings/branding/BrandingClient.tsx`, line 24:
Change `{ name: 'Default', primary: '#EEFF00', accent: '#09090b' }` to
`{ name: 'Default', primary: '#F52323', accent: '#09090b' }`

Only the `primary` field value changes. The `accent` value `#09090b` is intentionally
left unchanged — it is the dark accent, not the primary color.
</action>

<acceptance_criteria>
- `grep -n "EEFF00" src/app/(admin)/settings/branding/BrandingClient.tsx` returns zero matches
- `grep -n "Default" src/app/(admin)/settings/branding/BrandingClient.tsx` shows `#F52323` as primary
</acceptance_criteria>

</task>

---

## Task 5 — Update hero gradient and hover gradient in ClientPage.tsx

<task id="46-01-T5">

<read_first>
- src/app/(marketing)/ClientPage.tsx — line 253 (`via-yellow-200` in hero heading gradient)
- src/app/(marketing)/ClientPage.tsx — line 381 (`to-yellow-500/5` in card hover gradient)
</read_first>

<action>
In `src/app/(marketing)/ClientPage.tsx`, make two targeted changes:

1. Line 253: In the hero "built for service." heading span, change the gradient class
   `via-yellow-200` to `via-red-200`. The full class string changes from:
   `bg-gradient-to-r from-primary via-yellow-200 to-white`
   to:
   `bg-gradient-to-r from-primary via-red-200 to-white`

2. Line 381: In the card hover overlay div, change the gradient class
   `to-yellow-500/5` to `to-primary/5`. The full class string changes from:
   `bg-gradient-to-br from-primary/10 to-yellow-500/5`
   to:
   `bg-gradient-to-br from-primary/10 to-primary/5`

No other changes are made to ClientPage.tsx in this task. The `bg-primary text-zinc-950`
class replacements on lines 173, 275, 329, and 478 are handled in plan 46-02.
</action>

<acceptance_criteria>
- `grep -n "via-yellow-200\|to-yellow-500" src/app/(marketing)/ClientPage.tsx` returns zero matches
- `grep -n "via-red-200" src/app/(marketing)/ClientPage.tsx` shows one match on the hero span
- `grep -n "to-primary/5" src/app/(marketing)/ClientPage.tsx` shows one match on the hover overlay
</acceptance_criteria>

</task>

---

## Verification

### must_haves

- `--primary` CSS variable is `#F52323` in globals.css
- `--primary-foreground` CSS variable is `#ffffff` in globals.css
- Zero occurrences of `#EEFF00` anywhere in `src/` (tsx, ts files)
- Zero occurrences of `#CBFF00` anywhere in `src/`
- `via-yellow-200` and `to-yellow-500` are gone from ClientPage.tsx
- Admin branding palette default shows `#F52323`
- Superadmin cta_color fallback is `#F52323`

### verification_steps

```
# 1. Confirm no old hex values remain
grep -rn "EEFF00\|CBFF00" src/ --include="*.tsx" --include="*.ts" --include="*.css"

# 2. Confirm new hex value is present
grep -rn "F52323" src/ --include="*.tsx" --include="*.ts" --include="*.css" | wc -l

# 3. Confirm CSS variables
grep -n "primary" src/app/globals.css

# 4. Confirm gradients updated
grep -n "via-yellow-200\|to-yellow-500\|via-red-200\|to-primary/5" src/app/(marketing)/ClientPage.tsx

# 5. TypeScript check (run after 46-02 completes before committing)
npx tsc --noEmit
```

> NOTE: Do NOT commit after this plan. Both 46-01 and 46-02 must be committed together
> as a single atomic git commit. Committing 46-01 alone leaves `text-zinc-950` on
> red buttons, breaking WCAG contrast. The commit step is in 46-02.
