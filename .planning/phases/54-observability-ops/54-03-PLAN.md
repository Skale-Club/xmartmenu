---
phase: 54-observability-ops
plan: 03
type: execute
wave: 2
depends_on: ["54-01", "54-02"]
files_modified:
  - scripts/xphere-obs-check.ts
  - package.json
autonomous: true
requirements: [OBS-01, OBS-02]
must_haves:
  truths:
    - "An offline gate asserts the producer kill switch: with XPHERE_SYNC_ENABLED disabled, enqueueXphereSync makes ZERO publish (no fetch) and never throws"
    - "The same gate asserts the kill switch ON path still publishes exactly once (no regression to fail-open)"
    - "The gate structurally asserts the xphere-resync route gates on assertSuperadmin and calls enqueueXphereSync(id, 'manual')"
    - "The gate runs with no real creds, no network (fetch stubbed), and exits non-zero on any failed assertion"
  artifacts:
    - path: "scripts/xphere-obs-check.ts"
      provides: "tsx offline gate for OBS-01 resync route structure + OBS-02 producer kill switch"
      contains: "XPHERE_SYNC_ENABLED"
    - path: "package.json"
      provides: "npm run xphere:check:obs script wired to the new gate"
      contains: "xphere:check:obs"
  key_links:
    - from: "scripts/xphere-obs-check.ts"
      to: "src/lib/xphere/queue.ts"
      via: "dynamic import of enqueueXphereSync after setting env, fetch stubbed"
      pattern: "enqueueXphereSync"
    - from: "scripts/xphere-obs-check.ts"
      to: "src/app/api/superadmin/tenants/[id]/xphere-resync/route.ts"
      via: "readFileSync structural assertion (assertSuperadmin + 'manual')"
      pattern: "xphere-resync"
---

<objective>
Add an offline `tsx` gate that locks the two riskiest OBS invariants behind a runnable check (OBS-01 + OBS-02), with no creds and no network.

Purpose: Prove the producer kill switch silently no-ops when disabled (and still publishes when enabled), and that the resync route is superadmin-gated and calls enqueueXphereSync(id, 'manual') — mirroring the existing scripts/xphere-*-check.ts convention.
Output: `scripts/xphere-obs-check.ts` + `npm run xphere:check:obs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-observability-ops/54-CONTEXT.md

<interfaces>
<!-- Reuse the EXISTING gate conventions; do not invent a new harness. -->

Pattern to mirror — scripts/xphere-queue-check.ts:
- `import assert from 'node:assert/strict'`
- A `stubFetch(impl)` that records calls and replaces `globalThis.fetch`.
- Set dummy env (`QSTASH_TOKEN`, `XPHERE_WORKER_URL`) BEFORE `await import('@/lib/xphere/queue')`
  because queue.ts reads `QSTASH_TOKEN` at MODULE LOAD to build its client.
- `main().catch(err => { console.error(err); process.exit(1) })` wrapper.
- Restore `globalThis.fetch = originalFetch` after each block.

Pattern to mirror for the structural route assertion — scripts/xphere-backfill-check.ts:
- `readFileSync(new URL('../src/app/.../route.ts', import.meta.url), 'utf8')`
- `assert.match(src, /assertSuperadmin/, ...)` (the real guard needs cookies/Supabase, so assert structurally).

Producer kill-switch semantics implemented in Plan 02 (queue.ts):
- `isSyncEnabled()` = `XPHERE_SYNC_ENABLED` is truthy AND not 'false' AND not '0'.
- Disabled -> `enqueueXphereSync` returns BEFORE publish (zero fetch).
- queue.ts builds its QStash client from QSTASH_TOKEN at module load.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write scripts/xphere-obs-check.ts (kill-switch no-op + publish + route structure)</name>
  <files>scripts/xphere-obs-check.ts</files>
  <read_first>
    - scripts/xphere-queue-check.ts (the stubFetch helper, dummy-env-before-import setup, main().catch wrapper to copy)
    - scripts/xphere-backfill-check.ts (the readFileSync structural route assertion pattern)
    - src/lib/xphere/queue.ts (the kill-switch gate added in Plan 02 — assert its no-op/publish behavior)
  </read_first>
  <action>
    Create NEW file `scripts/xphere-obs-check.ts` (English JSDoc header explaining it gates OBS-01 resync route structure + OBS-02 producer kill switch, offline, no creds/network).

    Set dummy env BEFORE importing queue.ts:
    - `process.env.QSTASH_TOKEN = 'dummy-token-not-real'`
    - `process.env.XPHERE_WORKER_URL = 'https://example.test/api/internal/xphere-sync'`
    - `delete process.env.NEXT_PUBLIC_APP_URL`
    Then `const { enqueueXphereSync } = await import('@/lib/xphere/queue')`.

    Copy the `stubFetch(impl)` + `publishOk()` helpers from xphere-queue-check.ts (record calls, return a 200 JSON Response). Restore `globalThis.fetch` after each block.

    Assertion blocks:

    1. **Kill switch DISABLED — unset -> zero publish, no throw:**
       `delete process.env.XPHERE_SYNC_ENABLED`
       Stub fetch to throw if called. `await enqueueXphereSync('t1', 'manual')`.
       Assert: did NOT throw, and `calls.length === 0` (no publish when disabled).

    2. **Kill switch DISABLED — explicit 'false' and '0' -> zero publish:**
       For each of `'false'` then `'0'`: set `process.env.XPHERE_SYNC_ENABLED = <value>`, stub a recording fetch, `await enqueueXphereSync('t1', 'manual')`, assert `calls.length === 0`.

    3. **Kill switch ENABLED -> exactly one publish (no fail-open regression):**
       `process.env.XPHERE_SYNC_ENABLED = 'true'`; stub fetch returning `publishOk()`.
       `await enqueueXphereSync('t-enabled', 'manual')`.
       Assert `calls.length === 1`, and the published JSON body deep-equals `{ tenantId: 't-enabled', reason: 'manual' }`.

    4. **Resync route structural assertion (OBS-01):**
       `const src = readFileSync(new URL('../src/app/api/superadmin/tenants/[id]/xphere-resync/route.ts', import.meta.url), 'utf8')`.
       Assert: `assert.match(src, /assertSuperadmin/)`, `assert.match(src, /enqueueXphereSync\([^)]*'manual'/)`, `assert.match(src, /\{\s*ok:\s*true\s*\}/)`, `assert.match(src, /export\s+async\s+function\s+POST/)`.

    End with `console.log('xphere-obs-check: all assertions passed')`. Wrap in `main().catch(err => { globalThis.fetch = originalFetch; console.error(err); process.exit(1) })`.
  </action>
  <verify>
    <automated>npm run xphere:check:obs</automated>
  </verify>
  <acceptance_criteria>
    - File `scripts/xphere-obs-check.ts` exists.
    - grep: script contains `XPHERE_SYNC_ENABLED`, `assertSuperadmin`, `'manual'`, `xphere-resync`
    - grep: script contains `calls.length` (asserts publish count) and `process.exit(1)`
    - `npm run xphere:check:obs` exits 0 (after Task 2 wires the script)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The offline gate asserts: disabled kill switch (unset/'false'/'0') -> zero publish + no throw; enabled -> exactly one publish with the thin { tenantId, reason: 'manual' } body; and the resync route structurally gates on assertSuperadmin + calls enqueueXphereSync(id, 'manual') + returns { ok: true }.</done>
</task>

<task type="auto">
  <name>Task 2: Wire npm run xphere:check:obs</name>
  <files>package.json</files>
  <read_first>
    - package.json (the existing `scripts` block — mirror the `xphere:check:queue` entry format `npx tsx scripts/...`)
  </read_first>
  <action>
    Edit `package.json`. Add to the `scripts` object, alongside the other `xphere:*` entries:
    `"xphere:check:obs": "npx tsx scripts/xphere-obs-check.ts"`.
    Match the exact format of the sibling `xphere:check:queue` script. Do not reorder or change other scripts.
  </action>
  <verify>
    <automated>npm run xphere:check:obs</automated>
  </verify>
  <acceptance_criteria>
    - grep: package.json contains `xphere:check:obs` mapped to `npx tsx scripts/xphere-obs-check.ts`
    - `npm run xphere:check:obs` exits 0
  </acceptance_criteria>
  <done>`npm run xphere:check:obs` runs the gate and exits 0.</done>
</task>

</tasks>

<verification>
- `npm run xphere:check:obs` exits 0 (all OBS assertions pass).
- `npx tsc --noEmit` exits 0.
- Gate runs with no real creds and no network (fetch stubbed).
</verification>

<success_criteria>
- OBS-02: offline proof the producer kill switch no-ops when disabled and publishes once when enabled, fail-open intact.
- OBS-01: offline structural proof the resync route is superadmin-gated and calls enqueueXphereSync(id, 'manual').
- Gate wired as npm run xphere:check:obs; tsc clean.
</success_criteria>

<output>
After completion, create `.planning/phases/54-observability-ops/54-03-SUMMARY.md`
</output>
