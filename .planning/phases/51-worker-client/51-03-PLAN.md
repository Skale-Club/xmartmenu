---
phase: 51-worker-client
plan: 03
type: execute
wave: 3
depends_on: ["51-01", "51-02"]
files_modified:
  - src/lib/xphere/classify.ts
  - src/app/api/internal/xphere-sync/route.ts
  - scripts/xphere-worker-check.ts
  - package.json
autonomous: true
requirements: [FND-06]

must_haves:
  truths:
    - "the worker's retry classification is a pure, exported function exercised offline with no QStash/Xphere creds and no network"
    - "the offline check asserts: XphereTransientError -> 500, XpherePermanentError -> 489 + Upstash-NonRetryable-Error, disabled -> 200, success -> 200"
    - "the offline check asserts the fat-read -> buildSyncPayload wiring produces an external_id-keyed payload from fixture rows with a stubbed client (no real fetch)"
    - "npm run xphere:worker:check runs the script and exits non-zero on any failed assertion; tsc --noEmit stays green (scripts are typechecked)"
  artifacts:
    - path: "src/lib/xphere/classify.ts"
      provides: "pure classifyWorkerOutcome() mapping an outcome to { status, nonRetryable } — shared by the route and the offline check"
      contains: "Upstash-NonRetryable-Error"
    - path: "scripts/xphere-worker-check.ts"
      provides: "tsx offline assertion gate for retry-classification + fat-read->map wiring with a stubbed client"
      contains: "node:assert/strict"
    - path: "package.json"
      provides: "xphere:worker:check npm script"
      contains: "xphere:worker:check"
  key_links:
    - from: "src/app/api/internal/xphere-sync/route.ts"
      to: "src/lib/xphere/classify.ts"
      via: "route uses classifyWorkerOutcome to derive status/headers (single source of truth)"
      pattern: "classifyWorkerOutcome"
    - from: "scripts/xphere-worker-check.ts"
      to: "src/lib/xphere/classify.ts"
      via: "asserts the classification table offline"
      pattern: "classifyWorkerOutcome"
---

<objective>
Make the worker's retry-classification logic provably correct offline. Extract the transient/permanent/disabled/success → HTTP-status decision into a pure, exported `classifyWorkerOutcome()` in `src/lib/xphere/classify.ts`, have the Plan 02 route use it (single source of truth), then add `scripts/xphere-worker-check.ts` (extending the existing `scripts/` tsx + `node:assert/strict` convention) that asserts the full classification table AND the fat-read→`buildSyncPayload` wiring against fixture rows with a STUBBED client — no QStash, no Xphere creds, no network. Wire it as `npm run xphere:worker:check`.

Purpose: Lock the riskiest worker decision (retry vs DLQ vs ack) behind a runnable gate the same way Phase 50 locked the mapper — real signature/endpoint conformance is deferred to Phase 55.
Output: `src/lib/xphere/classify.ts`, an edit to the route to consume it, `scripts/xphere-worker-check.ts`, and an `npm run xphere:worker:check` script.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-worker-client/51-CONTEXT.md

@scripts/xphere-mapping-check.ts
@src/app/api/internal/xphere-sync/route.ts

<interfaces>
<!-- Use directly. -->

From src/lib/xphere/errors.ts (Plan 01):
```typescript
class XphereTransientError extends Error { status?: number }
class XpherePermanentError extends Error { status?: number }
```

From src/lib/xphere/client.ts (Plan 01):
```typescript
export type PostXphereResult = { disabled: true } | { disabled: false; response: XphereSyncResponse }
```

From src/lib/xphere/mapping.ts (Phase 50):
```typescript
export function buildSyncPayload(input: SyncMapInput): XphereSyncRequest  // external_id keyed on tenant.id
```

Existing offline-check convention (scripts/xphere-mapping-check.ts):
- `import assert from 'node:assert/strict'`, imports via `@/lib/...` path alias, plain fixtures, throws on any failed assertion (non-zero exit), ends with a `console.log('... all assertions passed')`. Reads NO env, imports NO dotenv/Supabase/QStash/fetch.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract pure classifyWorkerOutcome (classify.ts)</name>
  <read_first>
    - src/lib/xphere/errors.ts (XphereTransientError / XpherePermanentError — the classification inputs)
    - src/app/api/internal/xphere-sync/route.ts (Plan 02: the inline status/header decisions to lift out — 200 disabled, 200 success, 489+header permanent, 500 transient)
    - .planning/research/PITFALLS.md (Pitfall 9: transient → retry, permanent → DLQ; Pitfall 5: never advance on failure)
  </read_first>
  <files>src/lib/xphere/classify.ts</files>
  <behavior>
    - `classifyWorkerOutcome({ kind: 'success' })` -> `{ status: 200, nonRetryable: false }`
    - `classifyWorkerOutcome({ kind: 'disabled' })` -> `{ status: 200, nonRetryable: false }`
    - `classifyWorkerOutcome({ kind: 'tenant_gone' })` -> `{ status: 200, nonRetryable: false }`
    - `classifyWorkerOutcome({ kind: 'error', error: new XpherePermanentError('x') })` -> `{ status: 489, nonRetryable: true }`
    - `classifyWorkerOutcome({ kind: 'error', error: new XphereTransientError('x') })` -> `{ status: 500, nonRetryable: false }`
    - `classifyWorkerOutcome({ kind: 'error', error: new Error('unexpected') })` -> `{ status: 500, nonRetryable: false }` (unknown throws are treated as transient → retry)
    - `classifyWorkerOutcome({ kind: 'bad_payload' })` -> `{ status: 489, nonRetryable: true }`
  </behavior>
  <action>
    Create `src/lib/xphere/classify.ts` (English comments; pure — no I/O, no env, no network). Import `XphereTransientError`, `XpherePermanentError` from `./errors`.

    Export the type:
    ```ts
    export type WorkerOutcome =
      | { kind: 'success' }
      | { kind: 'disabled' }
      | { kind: 'tenant_gone' }
      | { kind: 'bad_payload' }
      | { kind: 'error'; error: unknown }
    export interface WorkerVerdict { status: 200 | 489 | 500; nonRetryable: boolean }
    ```

    Export `export function classifyWorkerOutcome(outcome: WorkerOutcome): WorkerVerdict`:
    - `success` / `disabled` / `tenant_gone` → `{ status: 200, nonRetryable: false }` (ack — no QStash retry).
    - `bad_payload` → `{ status: 489, nonRetryable: true }` (malformed message is permanent → DLQ).
    - `error` → if `outcome.error instanceof XpherePermanentError` → `{ status: 489, nonRetryable: true }`; else (XphereTransientError OR any other throw) → `{ status: 500, nonRetryable: false }`.

    Also export a tiny helper for the route to build the response headers: `export function nonRetryableHeaders(v: WorkerVerdict): Record<string,string> { return v.nonRetryable ? { 'Upstash-NonRetryable-Error': 'true' } : {} }`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -E "classifyWorkerOutcome|Upstash-NonRetryable-Error|XpherePermanentError" src/lib/xphere/classify.ts</automated>
  </verify>
  <acceptance_criteria>
    - classify.ts contains `export function classifyWorkerOutcome`
    - classify.ts contains `Upstash-NonRetryable-Error` (in nonRetryableHeaders)
    - classify.ts contains `XpherePermanentError` (the permanent → 489 branch)
    - classify.ts contains `status: 489` and `status: 500` and `status: 200`
    - classify.ts does NOT contain `fetch`, `process.env`, or `createServiceClient` (pure module)
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>A pure classifyWorkerOutcome + nonRetryableHeaders exist, mapping every outcome to the correct status + DLQ header.</done>
</task>

<task type="auto">
  <name>Task 2: Route consumes classifyWorkerOutcome (single source of truth)</name>
  <read_first>
    - src/app/api/internal/xphere-sync/route.ts (Plan 02: the inline 200/489/500 + header decisions)
    - src/lib/xphere/classify.ts (Task 1: classifyWorkerOutcome + nonRetryableHeaders)
  </read_first>
  <files>src/app/api/internal/xphere-sync/route.ts</files>
  <action>
    Refactor the Plan 02 route so its retry-classification responses are derived from `classifyWorkerOutcome` instead of inline literals — keeping behavior identical, just making the classification table the single source of truth shared with the offline check. Import `classifyWorkerOutcome`, `nonRetryableHeaders` from `@/lib/xphere/classify`.
    - bad_payload (zod parse fail) → `const v = classifyWorkerOutcome({ kind: 'bad_payload' })` then `NextResponse.json({ error: 'bad payload' }, { status: v.status, headers: nonRetryableHeaders(v) })`.
    - disabled / tenant_gone / missing-plan-permanent / success → derive `status` from the matching `classifyWorkerOutcome({ kind: ... })` (missing plan = `{ kind: 'error', error: new XpherePermanentError('no subscription') }` to reuse the 489 path).
    - catch block → `const v = classifyWorkerOutcome({ kind: 'error', error: err })` then return `NextResponse.json({ error: msg }, { status: v.status, headers: nonRetryableHeaders(v) })`.
    Do NOT change the verify-first logic, the 401 path, the fat-read, the write-back, or the env gate — only route the status/header decision through the shared classifier. The signature-verify 401 stays a direct literal (it is auth, not retry classification).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -E "classifyWorkerOutcome|nonRetryableHeaders" src/app/api/internal/xphere-sync/route.ts && grep -E "status: 401" src/app/api/internal/xphere-sync/route.ts</automated>
  </verify>
  <acceptance_criteria>
    - route.ts contains `classifyWorkerOutcome(` and `nonRetryableHeaders(`
    - route.ts still contains `status: 401` (signature-verify reject unchanged)
    - route.ts still contains `Receiver` and `runtime = 'nodejs'` (Plan 02 behavior preserved)
    - route.ts still contains `xphere_synced_at` and `xphere_sync_error` (write-back preserved)
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>The route derives its 200/489/500 + Upstash-NonRetryable-Error responses from the shared classifier; verify/write-back logic unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Offline worker check script + npm script</name>
  <read_first>
    - scripts/xphere-mapping-check.ts (THE convention to extend: node:assert/strict, @/lib import alias, fixtures, console.log on success, no env/network)
    - src/lib/xphere/classify.ts (Task 1: classifyWorkerOutcome — the table to assert)
    - src/lib/xphere/errors.ts (construct XphereTransientError / XpherePermanentError fixtures)
    - src/lib/xphere/mapping.ts (buildSyncPayload — assert fat-read fixtures map to an external_id-keyed payload)
    - package.json (scripts block: mirror "xphere:check": "npx tsx scripts/xphere-mapping-check.ts")
  </read_first>
  <files>scripts/xphere-worker-check.ts, package.json</files>
  <action>
    Create `scripts/xphere-worker-check.ts` (English comments; `import assert from 'node:assert/strict'`; import via `@/lib/...`; read NO env, import NO dotenv/Supabase/QStash; NO real `fetch`). Two assertion groups:

    GROUP A — retry classification (assert the full table from classify.ts):
    - `classifyWorkerOutcome({ kind: 'success' })` deepEquals `{ status: 200, nonRetryable: false }`
    - `{ kind: 'disabled' }` → 200 / false
    - `{ kind: 'tenant_gone' }` → 200 / false
    - `{ kind: 'bad_payload' }` → 489 / true
    - `{ kind: 'error', error: new XpherePermanentError('unknown stage') }` → 489 / true
    - `{ kind: 'error', error: new XphereTransientError('xphere 503') }` → 500 / false
    - `{ kind: 'error', error: new Error('boom') }` → 500 / false (unknown throw treated transient)
    - assert `nonRetryableHeaders` returns `{ 'Upstash-NonRetryable-Error': 'true' }` for a permanent verdict and `{}` for a transient one.

    GROUP B — fat-read → map wiring with a STUBBED client (no network):
    - Define a `stubPostXphereSync` that returns `{ disabled: false, response: { account_id: 'acc_1', contact_id: 'con_1', opportunity_id: 'opp_1' } }` WITHOUT any fetch (prove the worker's write-back inputs are correct without real I/O).
    - Build a fixture `SyncMapInput` (tenant + owner + plan + currency + reason='plan_activated', as in scripts/xphere-mapping-check.ts) and call `buildSyncPayload(fixture)`; assert `payload.account.external_id === tenant.id`, `payload.contact.external_id === tenant.id`, `payload.opportunity.external_id === tenant.id`, and `payload.source === 'xmartmenu'` — proving the fat-read shape the worker assembles maps to an external_id-keyed payload.
    - Call `stubPostXphereSync(payload)` and assert the returned `response.account_id/contact_id/opportunity_id` are the exact values the worker would persist to `xphere_account_id/contact_id/opportunity_id` (assert each string), proving the success write-back wiring with no creds.

    End with `console.log('xphere-worker-check: all assertions passed')`.

    In package.json `scripts`, add `"xphere:worker:check": "npx tsx scripts/xphere-worker-check.ts"` (mirror the existing `xphere:check` entry exactly).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run xphere:worker:check && grep '"xphere:worker:check"' package.json</automated>
  </verify>
  <acceptance_criteria>
    - scripts/xphere-worker-check.ts contains `node:assert/strict`
    - scripts/xphere-worker-check.ts contains `classifyWorkerOutcome` and `buildSyncPayload`
    - scripts/xphere-worker-check.ts contains `Upstash-NonRetryable-Error` (asserts the DLQ header)
    - scripts/xphere-worker-check.ts does NOT contain `fetch(`, `process.env`, `createServiceClient`, or `dotenv` (offline, no creds, stubbed client)
    - package.json contains `"xphere:worker:check"`
    - `npm run xphere:worker:check` exits 0 and prints `all assertions passed`
    - `npx tsc --noEmit` passes (scripts are typechecked — fixtures stay type-correct)
  </acceptance_criteria>
  <done>An offline tsx gate asserts the retry-classification table and the fat-read→external_id-keyed-map→write-back wiring with a stubbed client, runnable via npm run xphere:worker:check, with no real creds.</done>
</task>

</tasks>

<verification>
- `npm run xphere:worker:check` exits 0, prints "all assertions passed"
- `npx tsc --noEmit` passes (route + classify + both scripts typecheck)
- `grep -rn "fetch(\|process.env" scripts/xphere-worker-check.ts` returns nothing (offline, no creds)
- route.ts retry responses derive from classifyWorkerOutcome (shared with the check)
</verification>

<success_criteria>
- Retry classification (transient→500, permanent→489+Upstash-NonRetryable-Error, disabled/gone→200) is a pure function locked behind a runnable offline gate
- Fat-read→buildSyncPayload→write-back wiring is asserted with a stubbed client (no QStash/Xphere creds, no network)
- npm run xphere:worker:check is green and tsc --noEmit stays green
</success_criteria>

<output>
After completion, create `.planning/phases/51-worker-client/51-03-SUMMARY.md`
</output>
