title = "xmartmenu gitleaks config"

[allowlist]
description = "False positives — PASSWORD_CHARS is a character alphabet, not a secret"
regexes = [
  "PASSWORD_CHARS\\s*=\\s*['\"][A-Za-z0-9]+['\"]",
]
